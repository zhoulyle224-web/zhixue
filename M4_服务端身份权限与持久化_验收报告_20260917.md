# M4 服务端身份、权限与持久化验收报告

日期：2026-09-17

需求基线：`REQ-M4_服务端身份权限与持久化_20260917.md`

工作约束：`智学双擎_下一阶段模块拆分与Agent工作约束_20260916.md`
验收结论：**本地 Node + SQLite 演示版已完成服务端 opaque Session、角色/对象级授权、CSRF、会话持久化和公开端 fail-closed；M4-T01～T45、M1～M3 与 Skill 回归全部通过。该实现是比赛本地演示级身份方案，不等同于学校生产统一身份或公网生产安全方案。**

## 1. 修改文件

| 文件 | 新增/修改 | 修改原因 | 核心变化 |
|---|---|---|---|
| `db/runtime-schema.sql` | 修改 | 增加 M4 运行时身份模型 | 新增 schema meta、accounts、sessions、索引和约束 |
| `server/auth-service.mjs` | 新增 | 统一服务端认证与 Session 生命周期 | scrypt、随机 token/CSRF、Cookie、me、logout、expire、disabled/actor 校验 |
| `server/authorization.mjs` | 新增 | 集中对象级授权 | teacher offering/class、student self/enrollment、scoped catalog |
| `server/local-api.mjs` | 修改 | 所有受保护 API 接入 Session | auth API、CSRF、M1/M2/M3/export 授权、private snapshot、no-store |
| `server/task-service.mjs` | 修改 | 防止 M3 assignment/version IDOR | 暴露服务端内部 version/assignment scope 查询 |
| `assets/api-client.js` | 新增 | 统一浏览器会话调用 | me/login/logout、内存 CSRF、同源 Cookie、401 跳转 |
| `assets/app.js` | 修改 | 移除浏览器身份真相 | 不再比较密码或存 role；受保护数据无静态 fallback；安全 next/role switch |
| `assets/qa-client.js`、`assets/task-client.js` | 修改 | M1/M3 状态请求携带 CSRF | 统一经 authenticated fetch 调用 |
| `assets/style.css` | 修改 | 阻止受保护页面在会话确认前闪现 | teacher/student 工作台在 `/api/auth/me` 成功前保持隐藏 |
| `login.html` | 修改 | 准确说明服务端演示会话 | 加载 API client，更新安全说明和角色切换文案 |
| `teacher.html`、`student.html` | 修改 | 受保护页加载统一客户端 | 页面启动先 `/api/auth/me` 再加载 scoped 数据 |
| `scripts/export_frontend_snapshots.py` | 修改 | 关闭公开 actor 快照 | private 完整快照写 `data/`；public JSON 只保留课程名称/代码 |
| `assets/demo-data.json` | 生成更新 | 公开资源最小化 | 删除 teacher/student actor 快照、班级、人数、QA、任务 |
| `worker/index.js` | 修改 | 公开 Worker 未实现 auth 时安全失败 | login/protected API 返回 501，不再按可猜 context 返回 D1 dashboard |
| `tests/auth-test-helper.mjs` | 新增 | 旧回归适配 M4 契约 | 测试显式登录并携带 Cookie/CSRF，无测试后门 |
| `tests/m4-auth-permissions.test.mjs` | 新增 | 覆盖 M4-T01～T45 | 登录、Cookie、CSRF 恢复/轮换、权限矩阵、IDOR、重启、快照、Worker |
| M1/M2/M2.5/M3 既有测试 | 修改 | 预期安全契约已变化 | 显式 Session；越权断言改为 M4 403，原业务目的保留 |
| `package.json` | 修改 | 纳入回归门禁 | `npm test` 增加 M4 测试 |

未修改：`db/schema.sql`、`db/schema.ts`、`data/zhixue_demo.sqlite`、Skill、prompts、学校标识或真实账号数据。未新增生产依赖。

## 2. Auth 数据结构

### `runtime_auth_accounts`

字段：`id`、`account_name`（UNIQUE）、`role`、`actor_ref_id`、`actor_ref_code`、`password_algo`、`password_salt`、`password_hash`、`disabled`、`created_at`、`updated_at`。

- role 仅允许 teacher/student；
- 密码使用 Node `scryptSync` + 每账号 16 字节随机 salt + 32 字节派生值；
- 校验使用 `timingSafeEqual`；
- 不存在明文 password 字段。

### `runtime_auth_sessions`

字段：`id`、`account_id`、`session_token_hash`（UNIQUE）、`csrf_token_hash`、`created_at`、`last_seen_at`、`expires_at`、`revoked_at`、`remember_login`、`user_agent_hash`。

索引：

- `idx_runtime_auth_sessions_account(account_id, expires_at DESC)`；
- `idx_runtime_auth_sessions_active(session_token_hash, revoked_at, expires_at)`。

迁移通过幂等 `CREATE TABLE/INDEX IF NOT EXISTS` 执行；`runtime_schema_meta` 记录版本 4，不清表、不覆盖既有账号状态、不删除 M1/M2/M3 数据。

## 3. 演示账号映射

- `teacher2026 / demo123` → role teacher → `T0007 / teacher_id 7`；
- `student2026 / demo123` → role student → `S240101 / student_id 1`。

每次登录/Session resolve 都重新校验基线 actor 状态。密码**不明文落库**；公开凭据只作为比赛演示提示。

## 4. Session 与 CSRF

- Cookie：`zhixue_session`；
- 属性：`HttpOnly; SameSite=Strict; Path=/`，不设置 Domain；
- Cookie 值只包含随机 Session token，不包含 CSRF token；
- `ZHIXUE_COOKIE_SECURE=1` 时追加 `Secure`；本地 `127.0.0.1` HTTP 不强制；
- Session token 与 CSRF token 均由独立 `randomBytes(32)` 生成；数据库只保存 SHA-256；
- 普通会话 8 小时；remember login 7 天并设置 `Max-Age=604800`；
- `/api/auth/me` 从 HttpOnly opaque cookie 恢复 actor，每次生成新的随机 CSRF、替换当前 Session 的 `csrf_token_hash`，并仅在 JSON 响应中交给当前页面；旧 CSRF 随即失效；
- CSRF 不进入 Cookie、localStorage、sessionStorage 或日志；前端只保存在页面内存；
- 所有认证后的 POST/PUT/PATCH/DELETE（login 除外）要求 `X-CSRF-Token`；有 Origin 时同时做同源检查；
- logout 写入 `revoked_at`，原 Cookie 之后返回 401；
- 受保护响应统一 `Cache-Control: private, no-store`。

## 5. 服务端授权矩阵

### 教师

`Session teacher_id → course_offerings.teacher_id → offering_classes`。

- `teacher:7:1`：允许；
- `teacher:1:1`：403 `AUTH_CONTEXT_FORBIDDEN`；
- catalog 仅返回 T0007 的 offering/class，不返回其它教师课程；
- M2 import/confirm/analyze、M3 draft/publish/revise/revoke/feedback、M1 inbox/reply 全部复用同一 scope。

### 学生

`Session student_id → students + enrollments`。

- context 固定由 Session 推导为 `student:S240101`；
- 伪造 `student:S240102`：403；
- offering 必须存在当前学生非退选 enrollment；
- assignment complete 先验证 assignment.student_id，阻断 IDOR；
- 学生不能访问教师 dashboard、inbox、M2 或 M3 教师写接口。

### query/body

客户端 role/context/studentContext 只作为请求对象或一致性检查，不再是身份来源；改 URL、query、body 或 localStorage 不会获得额外权限。

## 6. M1 对接

- QA 提问、resources、history 使用 Session student actor 和 enrollment；
- teacher inbox/reply 使用 Session teacher scope；
- reply 还校验问题记录自己的 teacherContext；
- 旧 studentContext 仅允许与当前 Session 一致；
- M1 跨端待办、回复、PII、引用和幂等回归继续通过。

## 7. M2 对接

- import validate/confirm/analyze/latest/analysis by id 仅 teacher；
- context 先走 teacher offering/class 授权，再检查 batch/analysis 固有 context；
- 其它教师 context 在读取业务记录前即 403；
- 未改事实计算、analysisRun、学生映射或证据语义。

## 8. M3 对接

- draft/publish/revise/revoke 仅 teacher 且按版本所属 plan context 授权；
- student task/history 只使用当前 Session 本人；
- complete 同时验证 Session student_id 与 assignment.student_id；
- teacher 不能冒充学生完成；学生不能调用教师操作；
- 未改变任务正文、完成反馈和版本状态机业务规则。

## 9. 静态快照

`assets/demo-data.json` **不再包含** teacher/student actor 快照。

公开内容仅有：

- 合成数据版本元信息；
- 去重后的课程代码和课程名称。

完整 actor 级 read model 写入 `data/web_snapshots.json`，由本地服务私有读取；`/data/*` 静态路由继续 404。前端 401/403 时不会回退导出或展示公开 actor 快照。

## 10. Worker

公开 Worker **未实现完整 auth**，且不伪装为已实现：

- `POST /api/auth/login` → 501 `AUTH_NOT_AVAILABLE_IN_PUBLIC_DEMO`；
- `/api/auth/me`、catalog、dashboard、export → 501 fail closed；
- D1 可保留私有快照，但未认证 context 不再暴露 dashboard。

## 11. 基线数据库与 runtime 数据

`data/zhixue_demo.sqlite` 未发生变化。

验收前后 SHA-256：

`BBA0FC13A8332286BE5ACEF3E190FCB3ABF54F92648254C100073E83873922C4`

自动化使用 `:memory:` 或系统临时 runtime DB；M4-T41 在同一文件 DB 中预置 QA/import/task 后重启迁移，三类记录计数保持一致。开发 `data/runtime/` 未被清空或重建。

现有审计机制可能向 `data/runtime/audit.jsonl` 追加测试审计事件；事件不记录明文密码、Session token 或 CSRF token，不改写既有 SQLite 业务记录。

## 12. 自动化测试

| 门禁 | 结果 |
|---|---|
| `npm test` | **104/104 通过** |
| Skill verification | **26/26 通过** |
| M4-T01～T45 | **45/45 场景通过**（Node runner 含父测试共 46/46） |
| M1/M2/M2.5/M3 回归 | 全部通过 |
| `node --check`（api-client/app/qa/task/auth/authorization/local-api/runtime-store/task-service/worker/M4 test） | 全部通过 |
| `python -m py_compile scripts/export_frontend_snapshots.py` | 通过 |
| public snapshot security | 通过 |
| Worker dashboard fail closed | 通过 |
| baseline SHA-256 | 通过 |

M4-T01～T45 未 skip、未通过关闭 CSRF 或测试后门绕过。M4-T45 使用临时文件 runtime SQLite 验证服务重启后仅凭原 remember Session Cookie 重新取得 CSRF，新 CSRF 写入成功且旧 CSRF 返回 403。

## 13. 人工验收

使用本地临时 runtime DB 与两个 host 隔离的浏览器 Cookie 会话（`127.0.0.1` 学生、`localhost` 教师），未写开发 runtime DB。

| 场景 | 结果 | 证据摘要 |
|---|---|---|
| 未登录直开 teacher.html | 通过 | 自动跳 `login.html?role=teacher&next=teacher.html` |
| teacher 登录 | 通过 | 服务端登录后显示“演示教师” |
| teacher 合法范围 | 通过 | 仅显示 AI201 和 T0007 的 3 个关联班级 |
| student 登录 | 通过 | 显示学生001本人和 9 门已选课程 |
| 两会话同时存在 | 通过 | 两个 host-scoped Cookie 会话同时保持教师/学生工作台 |
| role switch | 通过 | 注销原会话并跳相应登录页，不改本地 role |
| localStorage 伪造 | 自动化通过 | 无可信 auth key；匿名 API 401 |
| teacher 越权 context | 自动化通过 | 403 |
| student teacher API / 他人 context | 自动化通过 | 403 |
| logout/restart | 自动化通过 | revoke 后 401；remember Session 重启后恢复 |
| public snapshot / Worker | 自动化通过 | 无 actor snapshot；Worker protected API 501 |

## 14. 完成定义 D01～D46

- D01～D44：由服务端实现、M4-T01～T44、原回归、静态扫描和哈希门禁覆盖；
- D45：两个相互隔离 Cookie 会话的教师/学生页面走查完成，越权矩阵由相同服务端的独立 Cookie Jar 集成测试补齐；
- D46：登录页明确说明“合成演示账号 + 本地服务会话，不等同学校生产统一身份”。

## 15. 未完成项与边界

1. 未接真实学校 CAS/OAuth2/SAML/LDAP，也未导入真实账号；
2. Worker 未实现完整 auth，故公开版受保护业务 API fail closed；
3. M5 的精细导出治理与更完整审计仍属下一阶段；
4. 本地 HTTP 演示不等同生产 HTTPS 安全方案；生产部署必须启用 HTTPS/Secure Cookie、学校统一身份、密钥管理和更严格运维控制；
5. 未开发 M5 或其它越界功能。

## 16. 结论

M4 已把身份边界从：

`浏览器本地 role/context → API 信任字符串`

改为：

`服务端校验账号 → opaque HttpOnly Session → 基线 SQLite 授课/选课关系 → 对象级授权 → M1/M2/M3 runtime 持久化`

修改 localStorage、URL role/context、studentContext 或 assignmentId 均不能提升权限；公开静态资源与 Worker 不再凭可猜 context 暴露受保护快照。
