# M5 受控导出与异常降级验收报告

验收日期：2026-09-17

需求基线：`REQ-M5_受控导出与异常降级_20260917.md`

上位约束：`智学双擎_下一阶段模块拆分与Agent工作约束_20260916.md`
结论：M5 D01～D50 已按本地 Node 正式导出边界实现并通过自动化及双角色人工验收。公开 Worker 不提供正式导出，只做默认拒绝。

## 1. 修改文件

| 文件 | 新增/修改 | 原因 | 核心变化 |
|---|---|---|---|
| `server/export-service.mjs` | 新增 | 建立正式导出安全边界 | 四类独立 DTO、授权后加载、二次脱敏、结构/最终扫描、四格式序列化、水印、摘要、5 MiB 门禁、严格审计 |
| `server/local-api.mjs` | 修改 | 替换旧 GET 导出链路 | `POST /api/export` 接入 M4 Session/CSRF；旧 GET 返回 405；统一处理 ExportError |
| `server/runtime-store.mjs` | 修改 | 提供 canonical audit 持久化 | 新增严格 `writeExportAudit` 和 `getExportAudit` |
| `db/runtime-schema.sql` | 修改 | 保存可查询且有唯一约束的导出审计 | 新增 `runtime_export_audits` 及 account/scope 索引，schema meta 升至 5 |
| `assets/export-client.js` | 新增 | 教师、学生共用受控导出交互 | 确认弹窗、格式选择、POST、错误状态、Blob 短期处理、显式公开样例入口 |
| `assets/app.js` | 修改 | 移除浏览器自行拼装业务文件 | 删除旧正式导出和学生 `JSON.stringify(state)` 下载，改为仅提供当前安全 scope |
| `assets/style.css` | 修改 | 支撑导出确认交互 | 新增受控导出弹窗、状态和安全提示样式 |
| `teacher.html` | 修改 | 接入正式导出客户端 | 加载 `assets/export-client.js` |
| `student.html` | 修改 | 接入本人学习记录正式导出 | 加载 `assets/export-client.js` |
| `worker/index.js` | 修改 | 公开版默认拒绝 | `/api/export` 返回 501 `EXPORT_NOT_AVAILABLE_IN_PUBLIC_DEMO` |
| `assets/samples/public-export-demo.json` | 新增 | 提供用户显式选择的安全结构样例 | 固定人工审查的公开合成数据，不关联 Session/runtime |
| `assets/samples/public-export-demo.csv` | 新增 | 提供 CSV 结构样例 | 固定公开合成样例，无 actor 标识 |
| `tests/m5-export-security.test.mjs` | 新增 | 建立 M5-T01～T50 门禁 | 覆盖授权、脱敏、注入、审计、格式、大小、落盘、Worker、基线等 |
| `tests/local-api.test.mjs` | 修改 | 适配 POST 合同 | 旧 GET 测试改为受控 POST 及新 DTO 断言 |
| `tests/auth-test-helper.mjs` | 修改 | 支撑真实 Session/CSRF 测试 | 测试请求按角色建立服务端会话 |
| `tests/m4-auth-permissions.test.mjs` | 修改 | 保持 M4 回归与 M5 合同一致 | 导出权限测试改走 POST；验证前端无受保护快照 fallback |
| `package.json` | 修改 | 将 M5 纳入回归门禁 | `npm test` 增加 M5 security test |

未手改 `dist/`、`.next/`、`.vinext/`、`.wrangler/`、`outputs/` 或 `work/`。

## 2. Export API

正式接口：

```text
POST /api/export
Content-Type: application/json
Cookie: M4 HttpOnly Session Cookie
X-CSRF-Token: M4 当前 Session 的 CSRF
```

请求结构：

```json
{
  "kind": "report | questions | review | learning-record",
  "format": "json | csv | excel | print",
  "scope": {
    "context": "teacher:<offeringId>:<classId>",
    "offeringId": 7
  }
}
```

- Teacher Session 仅允许 `report/questions/review`。
- Student Session 仅允许 `learning-record`。
- 教师 context 经服务端授课关系和班级关系授权；学生 offeringId 从 Session actor 校验本人 enrollment。
- 请求中的 `role/studentId` 不作为授权依据。
- `GET /api/export` 返回 405，且响应 `Allow: POST`。
- 正式响应包含 `Cache-Control: private, no-store`、`X-Content-Type-Options: nosniff`、`X-Zhixue-Export-Id` 和 `X-Zhixue-Watermark`。

## 3. 字段白名单

### 3.1 Teacher report

- `scope`: `courseCode`、`courseName`、`classRef`、`dataClassification`
- `analysis`: `analysisRunId`、`generatedAt`、`sampleSize`、`averageScore`、`passRate`、`excellentRate`、`knowledgeAnalysis`、`tierCounts`、`teachingSuggestions`
- `knowledgeAnalysis[]`: `knowledgePoint`、`masteryRate`、`mainErrorType`、`causeAnalysis`
- `teachingSuggestions`: `classUniversal`、`individualGuidance`、`nextTeachingFocus`
- `evidence`: `batchId`、`fileSha256`、`validRows`、`excludedRows`、`skillId`、`skillVersion`
- `taskSummary`: `versionNo`、`status`、`assignedCount`、`completedCount`、`completionRate`、`byTier`

不包含学生名单、完整学生编号、单人成绩、单条 feedback、原始 CSV 行、问题原文或教师内部备注。

### 3.2 Teacher questions

- `scope`: `courseCode`、`courseName`、`classRef`
- `questions[]`: `questionId`、`studentRef`、`question`、`status`、`assistantAnswer`、`evidence`、`teacherReply`、`createdAt`、`repliedAt`
- `evidence[]`: `title`、`locator`、`version`、`sourceLabel`

`studentRef` 使用 `S24***01` 形式；不输出 student_id、account_id、teacher_context、Session 或 SQL 内部关联字段。

### 3.3 Teacher review

- `scope`: `courseCode`、`courseName`、`classRef`
- `analysisSummary`: `analysisRunId`、`weakestKnowledgePoint`、`sampleSize`
- `teachingSuggestions`: 同 report
- `taskReview`: `versionNo`、`status`、`assignedCount`、`completedCount`、`completionRate`、`byTier`
- `qaSummary`: `totalQuestions`、`pendingTeacher`、`teacherReplied`
- `nextActions[]`: 从已存 M2 建议确定性组合

不包含学生列表、单条学生分数、问题正文或 raw feedback。

### 3.4 Student learning-record

- `student`: `studentRef`、`dataClassification`
- `courses[]`: `courseCode`、`courseName`、`qa`、`tasks`、`learningSummary`
- `qa[]`: `questionId`、`question`、`status`、`assistantAnswer`、`evidence`、`teacherReply`、`createdAt`、`repliedAt`
- `tasks[]`: `assignmentId`、`versionNo`、`versionStatus`、`tierCode`、`title`、`detail`、`dueAt`、`completionStatus`、`completedAt`、`feedback`、`weakestKnowledgePoint`
- `learningSummary`: `questionCount`、`taskCount`、`completedTaskCount`

student identity 只从 Session 推导；不包含其他学生或教师私有数据。

四类 DTO 最外层均由服务端增加固定顺序的 `exportMeta`：
`exportId`、`watermarkId`、`generatedAt`、`policyVersion`、`redacted`、`syntheticData`、`dataClassification`、`notice`。

## 4. 脱敏与内容安全策略

- Forbidden keys：拒绝 `password/token/cookie/session/csrf`、raw question/feedback、完整身份与其他安全敏感键；命中后 fail closed。
- PII 文本规则：导出时再次处理中国大陆手机号、邮箱、18 位证件号和完整合成学号模式。
- 学生引用：教师文件统一调用 `maskStudentRef`，完整编号变为遮罩引用。
- 结构扫描：安全 DTO 构造后递归检查字段名，未知敏感键注入会阻断导出。
- 最终扫描：四种格式序列化后均扫描最终文本；命中已定义敏感模式不返回 bytes。
- CSV/Excel：所有单元格执行 formula-safe，前导 `= + - @` 加单引号。
- Excel XML：文本再执行 XML entity escape。
- Print：用户文本执行 HTML escape，采用固定模板和可视水印。
- 公开表述不声称通用人名实体识别或“100% PII 识别”；当前演示数据为合成数据。

## 5. Watermark 与导出编号

- `exportId`：`exp_<UUID>`
- `watermarkId`：`ZX-YYYYMMDD-<8位随机十六进制>`
- policy：`export-policy-2026.09-v1`

同一导出的 watermarkId/exportId 在 JSON、CSV、Excel XML、Print HTML、响应头和 canonical audit 中一致。自动测试 M5-T17/M5-T18 验证一致性；人工教师导出也显示：

```text
exportId: exp_dd0eb362-2f78-43c5-bd47-c5c085b38da6
watermarkId: ZX-20260917-C2F06907
```

该人工证据来自已清理的临时合成 runtime，仅用于验收。

## 6. Canonical Audit

`runtime_export_audits` 字段：

```text
id, export_id, account_id, actor_role, actor_ref_code,
scope_type, scope_ref, export_kind, export_format,
watermark_id, policy_version, source_refs_json, record_count,
content_size_bytes, content_digest, result, failure_code,
generated_at, created_at
```

- `export_id`、`watermark_id` 均有唯一约束。
- `result` 仅允许 `generated/denied/failed`。
- `content_digest` 对最终返回 bytes 计算 `sha256:<hex>`。
- 审计只存技术引用和统计，不保存文件正文、问题/回复/反馈原文、姓名、手机号、邮箱、Token/Cookie/CSRF。
- canonical INSERT 在 HTTP 文件响应之前；INSERT 失败返回 503 `EXPORT_AUDIT_UNAVAILABLE`，不返回文件。
- JSONL 仅是 secondary 通用应用审计；在 canonical audit 成功后尝试写入，其失败不改变 canonical 事实。

人工临时库核对结果：

| actor | scope | kind/format | result | bytes | audit |
|---|---|---|---|---:|---|
| teacher | `teacher:7:6` | `report/json` | generated | 2882 | exportId、watermarkId、SHA-256 均存在 |
| student | `self` | `learning-record/json` | generated | 2584 | exportId、watermarkId、SHA-256 均存在 |

临时 runtime、脚本和服务已经清理。

## 7. Frontend fallback

教师正式导出失败后是否还会下载页面快照？

**否。**

- 401/403/503、network、脱敏、序列化、审计或服务失败只显示明确失败状态。
- 不读取 DOM、页面 state 或 localStorage 生成替代文件。
- 不自动下载公开样例。
- 重试会重新 POST，不复用旧 Blob。

## 8. Student export

是否还存在 `JSON.stringify(state.tasks/state.questions)` 直接下载？

**否。**

学生按钮固定打开 `learning-record` 受控导出，Session actor 决定本人身份；可选择全部授权课程或当前本人已选课程。页面实际走 `POST /api/export`。

## 9. Public sample

文件：

- `assets/samples/public-export-demo.json`
- `assets/samples/public-export-demo.csv`

两者包含固定结构演示数据与明显声明：

> 公开合成样例，仅展示文件结构，不对应任何当前账号、课程、班级或实时业务数据。

安全检查确认无 teacher/student actor 标识、完整学号、手机号、邮箱或正式 `ZX-` 水印。样例不读取 runtime，不随登录角色或页面状态变化。只能由用户在确认弹窗中显式点击“下载公开合成样例”，正式导出失败不会自动触发。

## 10. Worker

公开 Worker 是否实现正式 export？

**否。**

`/api/export` 返回 501 `EXPORT_NOT_AVAILABLE_IN_PUBLIC_DEMO`，默认拒绝，不从 D1 快照或静态页面拼装受保护文件。

## 11. 服务器文件落盘与基线数据库

- 正式文件：只在内存生成并直接响应，服务器不保存副本。
- 项目扫描结果：正式导出副本数量为 0。
- 最大输出：5 MiB；超限返回 413。
- `data/zhixue_demo.sqlite`：未修改。
- 验收后 SHA-256：`BBA0FC13A8332286BE5ACEF3E190FCB3ABF54F92648254C100073E83873922C4`
- 文件大小：9,932,800 bytes。
- runtime schema 采用幂等迁移；现有 M1/M2/M3/M4 记录不删除、不重置。

## 12. 自动化测试

| 命令/测试 | 状态 | 实际结果 |
|---|---|---|
| `node --test tests/m5-export-security.test.mjs` | 通过 | M5-T01～T50 全部通过；Node 统计 51/51 |
| `npm test` | 通过 | 155/155，0 failed |
| `node scripts/verify-skills.cjs` | 通过 | 26/26 |
| `node --check server/export-service.mjs` | 通过 | 无语法错误 |
| `node --check server/local-api.mjs` | 通过 | 无语法错误 |
| `node --check server/runtime-store.mjs` | 通过 | 无语法错误 |
| `node --check assets/app.js` | 通过 | 无语法错误 |
| `node --check assets/export-client.js` | 通过 | 无语法错误 |
| `node --check worker/index.js` | 通过 | 无语法错误 |
| `node --check tests/m5-export-security.test.mjs` | 通过 | 无语法错误 |

全量回归实测包括：

- M1 有据答疑、课程隔离、转教师、PII、重启与基线门禁；
- M2 导入、逐行质检、A/B 真实变化、重启与 5000 行边界；
- M2.5 analysisRun 固定快照、状态门禁与旧 runtime 迁移；
- M3 发布/完成/反馈/撤回/版本终态；
- M4 Session、权限、CSRF、重启恢复和静态暴露；
- M5 安全导出；
- Skill 契约 26/26。

## 13. 人工与自动验收矩阵

| 场景 | 验收方式 | 状态 | 证据 |
|---|---|---|---|
| Teacher report JSON | 浏览器人工 | 通过 | 确认弹窗后显示 generated、exportId、水印；canonical audit 对应一致 |
| Teacher questions CSV | 自动化 | 通过 | M5-T11/T12/T13/T15/T23/T27 |
| Teacher review Excel XML | 自动化 | 通过 | M5-T24/T28/T49 |
| Print HTML | 自动化 | 通过 | M5-T25/T26，HTML escape + 可视水印 |
| Student learning record JSON | 浏览器人工 | 通过 | 页面固定“本人学习记录”；generated、exportId、水印；canonical audit 对应一致 |
| Teacher 越权 context | 自动化 | 通过 | M5-T05 返回 403 |
| Student 请求 teacher export | 自动化 | 通过 | M5-T06 返回 403，无文件 |
| Node/API/503 失败 | 自动化+页面代码检查 | 通过 | M5-T22/T31/T32/T47；明确提示且无 snapshot/sample fallback |
| Audit failure | 自动化故障注入 | 通过 | M5-T21 返回 503，不返回 bytes |
| Redaction/scan failure | 自动化故障注入 | 通过 | M5-T16 fail closed |
| 5 MiB 超限 | 自动化故障注入 | 通过 | M5-T29 返回 413 并写 failed audit |
| Public sample | 人工 UI 检查+自动化 | 通过 | 弹窗仅显式链接；M5-T34/T35 验证静态独立与无 actor |
| Worker 正式 export | 自动化 | 通过 | M5-T36 返回 501，fail closed |
| 双角色人工链路 | 浏览器人工 | 通过 | 临时 runtime 上 teacher report 和 student learning-record 均成功；临时数据已清理 |

## 14. 完成项与明确边界

已满足 D01～D50，包括四格式安全测试、双角色人工导出、审计故障注入和公开样例边界。

按需求明确不实现：

- **原生 PDF**：未实现。当前为 Print HTML，用户使用浏览器打印功能另存 PDF。
- **原生 XLSX**：未实现。当前为 SpreadsheetML Excel 兼容 XML，扩展名 `.xls`。
- **Worker 正式导出**：未实现。公开版明确 501 fail closed。
- **通用人名实体识别**：未实现。采用字段白名单、已知 PII 规则与最终扫描；项目数据为合成数据。

未新增生产依赖，未修改 M1/M2/M3 业务语义，未修改 Skill 契约，未接入公网模型或真实学校数据。

## 15. 最终结论

M5 本地 Node 正式导出已形成：

```text
Session/CSRF
→ 服务端角色与范围授权
→ 最小字段白名单 DTO
→ 导出时二次脱敏
→ forbidden-key 与最终内容扫描
→ 安全格式化
→ 5 MiB 门禁
→ 单一水印/导出编号
→ 最终 bytes SHA-256
→ canonical SQLite 严格审计
→ 返回文件
```

权限、安全、序列化、审计或数据服务任一步失败均不产生未审查文件；浏览器不再使用页面/localStorage 作为正式导出替代；公开版只允许用户主动获取与当前业务无关的固定公开合成样例。
