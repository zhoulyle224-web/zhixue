# M4 CSRF 恢复最小补强验收报告

日期：2026-09-17

## 1. 原实现检查结论

原实现能够在服务重启后恢复 Session 和 CSRF，但方式是将 `Session token.CSRF token` 一并放入 HttpOnly Cookie。它不满足本次“CSRF 不进入 Cookie”的明确约束，因此需要最小修改，而不是只补测试。

## 2. 恢复与轮换机制

- `zhixue_session` Cookie 现在只保存随机 Session token，继续保持 `HttpOnly; SameSite=Strict; Path=/`；
- SQLite 的 `runtime_auth_sessions` 仍只保存 `session_token_hash` 和 `csrf_token_hash`；
- 有效 Session 调用 `GET /api/auth/me` 时，服务端重新生成 32 字节随机 CSRF token；
- 服务端立即用新 token 的 SHA-256 替换当前 Session 的 `csrf_token_hash`；
- 新 CSRF 明文只随 `/api/auth/me` JSON 响应返回，不进入 Cookie、SQLite 明文字段、localStorage、sessionStorage 或日志；
- `assets/api-client.js` 的既有 `me()` 会把响应数据写入页面内存；后续 POST/PUT/PATCH/DELETE 自动携带 `X-CSRF-Token`；
- Session 过期、revoked、账号 disabled 或 actor 无效仍按原规则返回 401/403，CSRF 恢复不会绕过身份校验。

未修改 M1/M2/M3 业务逻辑。

## 3. 新测试结果

新增 `M4-T45 remember Session 重启后重新获取 CSRF 并继续写操作`，使用系统临时目录中的文件型 runtime SQLite：

1. teacher 以 `rememberLogin=true` 登录；
2. 保存 Session Cookie 和旧 CSRF；
3. 关闭服务 A，使用同一临时 runtime SQLite 启动服务 B；
4. 新客户端状态仅携带原 Session Cookie；
5. `/api/auth/me` 返回 200、teacher actor 和新的非空 CSRF；
6. 新 CSRF 与旧 CSRF 不同；
7. 新 CSRF 调用受保护 POST 成功；
8. 旧 CSRF 调用同类写接口返回 403 `AUTH_CSRF_INVALID`；
9. SQLite 中保存值等于新 CSRF 的 SHA-256，且不等于 CSRF 明文；
10. Session Cookie 不含 CSRF token。

结果：**通过**。

## 4. 旧 CSRF 是否失效

是。`/api/auth/me` 完成重新签发后，数据库中的当前 Session `csrf_token_hash` 被替换，旧 CSRF 随即返回 403 `AUTH_CSRF_INVALID`。

## 5. 回归结果

- `npm test`：**104/104 通过**；
- M4-T01～T45：**45/45 场景通过**（Node runner 含父测试共 46/46）；
- M1/M2/M2.5/M3：全部通过；
- Skill：**26/26 通过**；
- `node --check server/auth-service.mjs`：通过；
- `node --check server/local-api.mjs`：通过；
- `node --check assets/api-client.js`：通过。

## 6. Schema 与数据迁移

未修改 runtime schema，无需数据迁移。既有 Session 行仍使用相同的 `session_token_hash`、`csrf_token_hash` 字段；首次成功调用 `/api/auth/me` 时只轮换该 Session 的 CSRF 哈希。测试使用临时 runtime DB，不污染正式运行数据。
