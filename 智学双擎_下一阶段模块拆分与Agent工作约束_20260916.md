# 智学双擎：下一阶段模块拆分与 Agent 工作约束

版本：2026-09-16  
适用基线：`智学双擎_完整项目_20260914.zip`  
上位依据：`智学双擎_下一阶段实施与验收规范_20260916.md`、赛题 5、评审标准及现有项目文档  
用途：作为下一阶段开发的总控文件。先确认模块拆分与实施边界，再为每个模块分别编写详细需求文件。

---

## 0. 总目标

下一阶段不继续扩张功能数量，重点把已有“学生课后答疑 + 教师学情研判”两条主场景做成可验证的真实闭环：

- 输入是真的；
- 计算是真的；
- 引用是真的；
- 状态变化是真的；
- 跨账号回流是真的；
- 权限判断由服务端真正执行；
- 异常和降级可复现；
- 文档、视频、PPT 与实际版本一致。

本阶段不把行政办公、课堂互动、课程内容迭代等扩展方向重新拉回主开发线。它们可以保留已有 Skill 或方案说明，但不得抢占核心闭环的开发和验收资源。

---

# 第一部分：下一阶段大模块拆分

## 模块总览

建议拆分为 **7 个大模块**。每个模块后续单独形成一份需求文件，避免一次性让 Agent 大范围修改项目。

| 编号 | 模块 | 主要目标 | 优先级 | 主要评分关联 |
|---|---|---|---|---|
| M1 | 学生答疑与课程证据链 | 修通课程隔离、真实检索、引用和无依据转人工 | P0 | 场景适配、功能实用、异常处理 |
| M2 | 数据导入、质检与真实研判 | 让 CSV/JSON 真解析、真质检、真计算，换数据结果必变化 | P0 | 功能实用、代码规范、异常处理 |
| M3 | 教学任务与反馈闭环 | 教师确认发布、学生接收完成、教师看到反馈并可撤回 | P0 | 场景适配、功能闭环、落地价值 |
| M4 | 服务端身份、权限与持久化 | 服务端会话、班级授权、跨会话共享、刷新/重启不丢状态 | P0 | 代码规范、安全、落地价值 |
| M5 | 受控导出与异常降级 | 鉴权、脱敏、水印、审计、失败默认拒绝、离线状态明确 | P1 | 数据安全、异常处理、实用性 |
| M6 | 自动化测试与验收证据 | 补跨账号、越权、导入变化、任务回流、故障等测试 | P1 | 代码规范、异常处理、可复现 |
| M7 | 参赛交付与评分证据 | 部署、视频、PPT、测试证据与实际功能严格一致 | P1 | 文档完整性、答辩可验证性 |

推荐实施顺序：

`M2 → M1 → M3 → M4 → M5 → M6 → M7`

说明：

1. M2 最先做，因为“换一个文件，统计结果立即改变”是最容易证明系统不是固定 Demo 的证据。
2. M1 第二，因为它同时验证 Skill、知识库、课程隔离、引用和幻觉控制。
3. M3 + M4 把两个角色连成真实教学闭环。
4. M5-M7 在核心业务链路稳定后收口，避免过早做包装。

---

## M1：学生答疑与课程证据链

### 目标

把当前学生答疑从“前端规则回退或固定资料效果”升级为：

`学生选择课程 → 输入自然语言问题 → 检索当前课程资料 → Skill 生成带证据回答 → 无依据则创建教师待办`

### 范围

- 当前课程上下文必须真实参与检索。
- 不同课程严格隔离，切换课程不得沿用上一门课程的固定材料。
- 引用必须定位到系统中实际存在的课程资料，例如：
  - 资源 ID；
  - 课程；
  - 章节；
  - 页码/段落；
  - 资源版本。
- 如果使用的是合成/演示课程资料，应显著标注“演示/合成资料”，不得写成真实院校课程资源。
- 无足够依据时返回“待人工处理”，不得补造引用。
- 教师待办必须进入持久化链路，不能只保存在学生浏览器 `localStorage`。
- 保持现有 Skill 输入输出契约稳定；确需扩展时采用向后兼容方式。

### 主要涉及现有区域

- `assets/app.js`
- `assets/skills/course-ai-tutor.js`
- `assets/skills/skill-registry.js`
- `server/local-api.mjs`
- `data/zhixue_demo.sqlite`
- `data/web_snapshots.json`
- 必要时新增服务端模块或表结构

### 核心验收

- 切换两门不同课程后，相同问题的检索范围和引用发生合理变化。
- 引用可追溯到实际存在的资源。
- 无资料问题不编造答案，进入教师待办。
- 教师使用另一浏览器/会话可看到该待办。
- 空问题、异常输入、注入类输入仍按约定错误处理。

### 不在本模块做

- 不做通用互联网搜索。
- 不为了“回答更聪明”强制接公网模型。
- 不把帝王蟹平台“历史验证”表述为当前网站实时调用。
- 不新增与主场景无关的问答机器人。

### 后续需求文件建议名

`REQ-M1_学生答疑与课程证据链.md`

---

## M2：数据导入、质检与真实研判

### 目标

把教师端“导入 → 质检 → 研判”做成真实数据驱动链路：

`上传 CSV/JSON → 服务端解析 → 逐行校验 → 教师确认 → 分析 Skill 计算 → 页面展示事实与建议`

### 范围

- 支持明确约定格式的 CSV/JSON。
- 服务端或可信业务层实际解析数据，不允许仅在页面模拟“已导入”。
- 至少识别：
  - 空值；
  - 重复；
  - 分数越界；
  - 类型/格式错误；
  - 必填字段缺失。
- 质检问题必须给出可定位信息，至少包含行号或记录标识。
- 保留导入批次、文件哈希、质检结果、确认状态。
- 教师确认后才进入正式研判。
- 均分、样本数、知识点表现、分层等事实必须由数据计算得出。
- 教学建议与客观事实分开展示，并标注依据/版本/更新时间。
- 页面“开始研判”必须真正调用分析能力。
- 换文件后计算结果必须随输入数据变化。

### 主要涉及现有区域

- `server/local-api.mjs`
- `assets/skills/academic-performance-analyzer.js`
- `assets/app.js`
- `db/schema.sql` / `db/schema.ts`
- `data/`
- `tests/`

### 核心验收

至少准备两份内容明显不同的输入文件 A/B：

- A 导入后得到一组均分、知识点、分层结果；
- B 导入后上述结果合理改变；
- 异常文件能定位错误行；
- 未确认数据不得伪装成正式研判结果；
- 数据不足时明确提示，不生成虚假结论。

### 不在本模块做

- 不追求无限兼容任意 Excel/CSV 格式。
- 不把目标指标或设计示例写成已验证结果。
- 不允许为了展示效果使用固定均分、固定弱项、固定分层。

### 后续需求文件建议名

`REQ-M2_数据导入质检与真实研判.md`

---

## M3：教学任务与反馈闭环

### 目标

把教师研判后的建议变成可流转、可追踪、可撤回的教学任务：

`教师确认研判 → 发布任务 → 学生收到 → 学生完成/反馈 → 教师查看结果 → 结果进入后续研判`

### 范围

- 教师必须先确认再发布。
- 任务记录至少包含：
  - 任务 ID；
  - 发布版本；
  - 发布人；
  - 课程/班级/学生范围；
  - 发布时间；
  - 状态；
  - 撤回信息。
- 学生端只能看到本人有权限看到的任务。
- 学生完成后产生持久化反馈。
- 教师端可查看完成状态和结果摘要。
- 支持撤回，并保留版本/审计轨迹。
- 刷新页面后状态不得丢失。
- 两个独立浏览器会话能看到一致的服务端状态。

### 主要涉及现有区域

- `assets/app.js`
- `server/local-api.mjs`
- `db/`
- `data/zhixue_demo.sqlite` 或运行时持久化数据库
- `tests/`

### 核心验收

完整执行一次：

1. 教师生成研判；
2. 教师确认并发布任务；
3. 学生在独立会话中收到任务；
4. 学生提交完成结果；
5. 教师刷新后看到反馈；
6. 教师撤回某一任务版本；
7. 历史版本仍可追溯。

### 不在本模块做

- 不做即时聊天、消息推送等非必要实时功能。
- 不做复杂 LMS 全量能力。
- 不用 `localStorage` 作为跨角色业务记录的最终事实源。

### 后续需求文件建议名

`REQ-M3_教学任务与反馈闭环.md`

---

## M4：服务端身份、权限与持久化

### 目标

把当前演示认证从“前端角色变量”提升为最低可接受的服务端身份与权限体系。

### 范围

- 演示账号也由服务端建立会话。
- 服务端是最终权限判断方。
- 不信任前端传入的：
  - `role`
  - `classId`
  - `studentId`
  - `context`
  - URL 查询参数
- 学生只能访问本人允许的数据。
- 教师只能访问所授班级/课程。
- 修改 URL、请求体或前端变量不得实现越权。
- 跨浏览器业务状态使用服务端持久化。
- 服务重启后关键记录仍存在。
- 公开静态资源中不得打包受保护明细或权限快照。
- 身份模型可以是比赛演示级，不要求本阶段接 CAS/OAuth/SAML，但不得把前端变量假装成生产鉴权。

### 主要涉及现有区域

- `server/local-api.mjs`
- 可新增 `server/auth-*`、`server/repository-*` 等清晰模块
- `db/`
- `assets/app.js`
- `worker/index.js`（仅在明确做公开版能力时）
- `tests/`

### 核心验收

- 学生请求教师看板返回拒绝。
- 教师访问非所授班级返回拒绝。
- 修改 URL 中 `role/context` 无法绕过权限。
- 两个独立会话可共享有权限的业务记录。
- 刷新和服务重启后记录仍存在。
- 受保护数据没有出现在公开静态构建产物中。

### 不在本模块做

- 不强行接入真实学校统一身份系统。
- 不添加真实学校账号或任何可识别学校信息。
- 不使用前端隐藏按钮代替权限控制。

### 后续需求文件建议名

`REQ-M4_服务端身份权限与持久化.md`

---

## M5：受控导出与异常降级

### 目标

保证“能导出”与“安全导出”同时成立；服务失败时默认不泄漏。

### 范围

- 导出前执行服务端鉴权。
- 按业务范围过滤字段。
- 敏感字段脱敏。
- 添加水印或可追溯标识。
- 写审计记录，至少包含：
  - 操作者；
  - 时间；
  - 导出范围；
  - 格式；
  - 结果；
  - 水印/追踪 ID。
- 审计日志不得记录敏感原文。
- 权限失败、脱敏失败、生成失败时不得产生未审查下载。
- API 失败时不得自动把页面中的明细 JSON 当正式导出替代品。
- 对公开合成数据，可保留明确标识的离线样例导出。
- 页面必须明确显示在线/本地/离线/降级状态，不得把降级结果伪装成正常服务结果。

### 主要涉及现有区域

- `server/local-api.mjs`
- `assets/app.js`
- `data/runtime/`
- `tests/`

### 核心验收

- 合法教师可导出授权范围数据。
- 学生导出班级数据失败。
- 教师导出非所授班级失败。
- 导出结果中敏感字段被处理。
- API/脱敏服务故障时不产生未审查文件。
- 审计可追踪但不泄露敏感内容。

### 后续需求文件建议名

`REQ-M5_受控导出与异常降级.md`

---

## M6：自动化测试与验收证据

### 目标

让“功能完成”必须有自动化或人工验证证据，杜绝文档先于代码宣布完成。

### 范围

在保持现有测试通过基础上，新增至少以下测试：

- 新文件导入后核心指标变化；
- 数据异常逐行定位；
- 不同课程答疑不串资料；
- 无依据问题进入教师待办；
- 教师发布 → 学生完成 → 教师看到反馈；
- 撤回任务并保留版本；
- 学生伪造教师请求被拒绝；
- 教师越权访问非所授班级被拒绝；
- API 故障时正式导出不泄漏；
- 受保护数据不进入公开静态资源。

测试状态统一使用三种：

- `自动化已覆盖`
- `人工已验证`
- `尚未实现`

不得把“写在测试文档里的用例”自动视为“已经通过”。

### 必须保留的现有门禁

现有工程已定义：

```bash
npm test
node scripts/verify-skills.cjs
node --check assets/app.js
node --check server/local-api.mjs
```

其中现有 `npm test` 覆盖本地 API / 页面基础测试，并执行 Skill 契约验证。新增开发不得破坏当前已通过能力。

### 后续需求文件建议名

`REQ-M6_自动化测试与验收证据.md`

---

## M7：参赛交付与评分证据

### 目标

让评委看到的材料与实际运行能力一一对应，不出现“材料写满、功能没做”的情况。

### 范围

建立“评分项 → 功能 → 测试 → 演示画面 → 证据文件”的映射。

建议至少维护：

| 官方评分项 | 必须能展示的证据 |
|---|---|
| 场景适配性 | 学生答疑、教师研判、任务回流真实流程 |
| 功能实用性 | 新文件导入后结果变化；无依据转人工；任务完成回流 |
| 操作便捷性 | 教师三步、学生三步完成核心任务 |
| 代码规范性 | Skill 契约、服务端 API、权限边界、自动测试 |
| 异常处理能力 | 异常文件、越权请求、无依据、API/导出故障 |
| 文档完整性 | 源码、一键部署、测试证据、视频、PPT |

### 学校信息红线

正式提交物中均不得出现可识别学校信息，包括但不限于：

- 学校名称；
- 校徽；
- 学校域名；
- 真实组织名称；
- 可识别院系标识；
- 真实教师/学生姓名、学号；
- 可反推出学校身份的截图、文件名、账号或路径。

此约束覆盖：

- PPT；
- 演示视频；
- README；
- 源代码示例；
- 数据库样例；
- 测试截图；
- 页面内容；
- 导出文件；
- 提交压缩包中的文档与资源。

### 后续需求文件建议名

`REQ-M7_参赛交付与评分证据.md`

---

# 第二部分：模块间依赖与实施边界

## 1. 依赖关系

```text
M2 数据导入/研判 ─┐
                  ├─> M3 教学任务闭环 ─> M4 权限与持久化
M1 学生答疑 ──────┘                    │
                                       ├─> M5 安全导出/降级
                                       └─> M6 测试验收
                                             │
                                             └─> M7 参赛交付
```

实际开发时，M4 的权限边界应从 M1/M2 开始同步设计，但大规模身份改造不要阻塞最先验证的业务链路。

---

## 2. 每个模块的“完成”定义

一个模块只有同时满足以下条件才可标记“完成”：

1. 代码已实现；
2. 页面实际走真实链路；
3. 正常场景验收通过；
4. 至少一个异常/边界场景通过；
5. 对应自动化测试已补充，或明确记录人工验收步骤；
6. 原有测试未回归失败；
7. 文档表述与实际实现一致；
8. 未引入新的权限、隐私、静态暴露风险。

仅满足以下任一情况不得写“已完成”：

- 页面有按钮但无真实执行；
- 使用固定 JSON 假装计算结果；
- 使用 `localStorage` 假装跨账号同步；
- 只写接口但页面未调用；
- 只写测试用例但没有执行；
- 只有设计图/目标指标；
- 只有模拟截图；
- 文档声称线上已支持但当前部署实际上没有该能力。

---

# 第三部分：Agent 工作总约束

以下约束用于后续任何编码 Agent、自动化 Agent 或协作 Agent。除非需求文件明确授权，否则一律遵守。

---

## A. 工作区与文件系统约束

### A1. 工作根目录

唯一允许作为项目修改目标的工作根目录：

`zhixue-dual-engine-frontend/`

Agent 必须先确认自己位于该项目根目录，再开始修改。

### A2. 禁止修改工作区外文件

Agent **不得创建、修改、移动、删除、重命名工作区外的任何文件或目录**，包括但不限于：

- 用户主目录其他文件；
- 系统配置；
- 其他项目；
- 父目录中的比赛资料；
- 原始赛题附件；
- 其他压缩包；
- 临时下载目录中的无关文件。

如某项操作必须依赖工作区外资源，应停止该操作并报告，不得自行扩大权限范围。

### A3. 基线压缩包只读

`智学双擎_完整项目_20260914.zip` 是历史基线证据：

- 不得覆盖；
- 不得重新打包写回同名文件；
- 不得删除；
- 不得把修改结果偷偷写入原 ZIP。

需要工作副本时，应解压到明确的新工作目录。

### A4. 不得修改赛题原文和评分附件

赛题 5、附件 3、附件 4等官方材料只作为依据：

- 不得编辑原文件；
- 不得替换原文件；
- 不得为了让项目“显得符合”而修改评分文本。

---

## B. 源文件与生成物约束

### B1. 优先修改源文件，不手改生成物

下列内容视为构建/派生产物，原则上不得直接手工编辑：

- `dist/`
- `.next/`
- `.vinext/`
- `.wrangler/`
- `outputs/`
- `work/`

需要更新时，修改源文件并执行正式构建脚本。

### B2. 快照与数据库通过既有脚本生成

涉及以下派生数据时，优先使用已有脚本：

```bash
python scripts/build_demo_database.py
python scripts/check_demo_database.py
python scripts/export_frontend_snapshots.py
python scripts/build_d1_migration.py
npm run build:demo
```

不得同时手工修改：

- SQLite 基础数据；
- 前端快照；
- D1 迁移；
- 静态 demo 数据，

从而造成四份数据相互不一致。

### B3. 不随意修改锁文件和依赖

除非模块需求明确需要：

- 不新增 npm 包；
- 不执行无必要的依赖大版本升级；
- 不重写 `package-lock.json`；
- 不更换框架；
- 不用“重新搭一套应用”代替在当前项目上修复。

如果确实必须新增依赖，应在变更说明中写明：

- 为什么现有 Node/项目能力无法满足；
- 新依赖用途；
- 安全和离线部署影响；
- 是否影响 `npm install` / 本地无网运行。

---

## C. 最小改动与兼容性约束

### C1. 最小改动原则

Agent 应优先：

- 扩展现有 API；
- 扩展现有 Skill；
- 拆分过大的现有模块；
- 复用现有 schema / 错误码 / 数据结构。

不得为了一个小需求另起一套互相矛盾的实现。

### C2. 保持 Skill 契约稳定

现有 Skill 的：

- 名称；
- 输入字段；
- 输出字段；
- 错误状态；
- 引用/证据字段；

除非需求明确允许，不得破坏性修改。

需要扩展字段时优先采用可选字段、版本字段或兼容转换。

### C3. 保持现有基础能力回归通过

任何模块修改后，不能只验证新功能。

至少检查：

- 健康检查仍正常；
- 课程目录/看板仍可用；
- 现有 Skill 契约仍通过；
- 静态资源不暴露数据库；
- 原有合法导出路径未被误伤；
- 教师/学生页面可以正常加载。

---

## D. 数据、安全与隐私约束

### D1. 仅使用合成/匿名数据

除非项目负责人明确提供并授权：

- 不引入真实学生数据；
- 不引入真实教师数据；
- 不引入真实学校内部资料；
- 不把互联网上找到的学校数据作为“试点数据”。

所有演示数据必须可说明来源和性质。

### D2. 不得引入学校识别信息

开发、测试、截图、默认账号、页面标题、文件名、导出样例中都不得新增可识别学校信息。

### D3. 服务端最终鉴权

禁止以下做法：

- 只隐藏前端按钮；
- 只检查 URL 中 `role=teacher`；
- 只相信请求体中的 `studentId/classId`；
- 通过客户端 `localStorage` 决定敏感数据访问权限。

### D4. 默认拒绝

当以下环节失败时：

- 权限无法确认；
- 脱敏失败；
- 数据范围无法确认；
- 引用无法验证；
- 导出审查失败；

默认结果必须是拒绝或转人工，而不是继续输出潜在敏感/无依据结果。

### D5. 日志不得记录敏感原文

审计与调试日志不得写入：

- 明文姓名/学号；
- 手机号；
- 原始敏感作答；
- 密钥；
- Token；
- Cookie；
- 完整会话凭证。

---

## E. 业务真实性约束

### E1. 禁止固定结果伪装真实能力

不得使用固定文案、固定统计值、预制 JSON 或 CSS 切换，冒充：

- 数据质检；
- 学情计算；
- 课程检索；
- 跨账号状态同步；
- 权限校验；
- 导出审计。

### E2. 输入变化必须影响结果

以下输入发生变化时，对应输出应合理变化：

- 课程变化 → 检索范围/引用变化；
- 文件变化 → 学情指标变化；
- 教师发布范围变化 → 学生任务变化；
- 学生完成状态变化 → 教师反馈变化；
- 当前身份变化 → 可访问资源变化。

### E3. 事实与建议分开

页面和 API 中：

- 均分、样本数、通过率等为计算事实；
- 薄弱点解释、教学建议等为分析/建议；

不得混成无法判断来源的一段生成文本。

### E4. 无证据不回答

学生答疑无可核实课程资料时：

- 不编造引用；
- 不把模型常识当课程资料；
- 返回待人工处理或明确的资料不足状态。

---

## F. 平台与能力表述约束

### F1. 三种状态必须区分

材料和页面描述中严格区分：

1. **本地 Skill 实现**
2. **帝王蟹平台历史验证**
3. **当前网站实时调用帝王蟹平台**

没有可核验调用记录，不得写“已接入帝王蟹”“在线调用帝王蟹”。

### F2. 不得把目标写成结果

例如：

- “目标引用召回率 90%” ≠ “已达到 90%”
- “计划 5 名教师测试” ≠ “5 名教师已验证”
- “支持设计” ≠ “功能已上线”
- “测试用例存在” ≠ “测试已通过”

---

## G. 网络、依赖与外部服务约束

### G1. 本地优先

当前项目的本地部署与离线演示能力是核心资产。

除需求明确要求，不得：

- 强制依赖公网 API；
- 把现有本地 Skill 改成必须联网才能运行；
- 为了开发方便引入无法离线安装的关键链路。

### G2. 禁止自行上传项目数据

Agent 不得把以下内容上传到外部服务：

- 源代码；
- SQLite 数据库；
- 测试数据；
- 日志；
- 用户文件；
- 比赛材料。

除非项目负责人明确授权特定外部服务和范围。

### G3. 不保存密钥

任何密钥、Token、Cookie 不得硬编码进：

- JS；
- TS；
- JSON；
- Markdown；
- 测试；
- 示例文件。

---

## H. 数据库与迁移约束

### H1. 不破坏历史数据

涉及表结构变化时：

- 使用迁移；
- 明确新增/变更字段；
- 不直接删表重建来规避兼容问题；
- 不删除已有演示数据以“修好测试”。

### H2. 运行时数据与基线数据分离

需区分：

- 可重建合成基线数据；
- 用户运行时产生的导入批次、待办、任务、反馈、审计。

后者不得在普通页面刷新或静态重建时被清空。

### H3. 删除/重置属于高风险动作

以下操作没有需求文件明确授权不得执行：

- 删除数据库；
- 清空表；
- 批量覆盖数据；
- 重置全部任务；
- 删除审计记录。

---

## I. 测试与质量门禁

### I1. 修改前先确认基线

Agent 开始一个模块前应记录：

- 当前工作区状态；
- 当前测试状态；
- 与本模块相关的关键文件。

如果基线本身存在失败，应先报告，不得把原有失败算成自己的修改结果。

### I2. 修改后必须执行对应测试

最少要求：

```bash
npm test
node scripts/verify-skills.cjs
node --check assets/app.js
node --check server/local-api.mjs
```

如果修改了其他 JS/TS 文件，应增加对应语法、lint 或测试。

### I3. 不得篡改测试来掩盖失败

禁止：

- 删除失败断言；
- 把真实比较改成恒真；
- `skip` 核心用例；
- 降低权限测试要求；
- 将异常预期改成成功，只为让 CI 变绿。

测试确需调整时，必须是因为需求/契约合法改变，并同步说明原因。

### I4. 测试证据真实记录

Agent 最终报告必须区分：

- 已运行且通过；
- 已运行但失败；
- 未运行；
- 仅人工检查。

不得写“应该通过”。

---

## J. UI 与交互约束

### J1. 保持低门槛主流程

核心路径维持：

教师：

`选班级 → 确认数据 → 生成研判`

学生：

`选课程 → 提问 → 查看引用`

新增能力不得让核心流程明显变复杂。

### J2. 状态必须可见

至少明确显示：

- 加载中；
- 空数据；
- 失败；
- 离线/降级；
- 待教师确认；
- 已发布；
- 已撤回。

### J3. 不以动画替代反馈

动态效果不得遮挡关键结果，也不得以“成功动画”代替真实服务端成功状态。

---

## K. 文档与提交约束

### K1. 代码与文档同步

任何模块完成后应同步更新与其直接相关的：

- API 说明；
- 测试说明；
- 部署步骤；
- 已实现/未实现状态。

但不要在模块开发中顺手大改所有文档。

### K2. 不提前宣布完成

视频、PPT、真实测试证据未生成前：

- 不给文档完整性写满分；
- 不把规划中的画面写成已录制；
- 不把待验证功能写成“已稳定运行”。

### K3. 不修改官方评分文本

可以增加“项目如何对应评分”，但不得改写官方分值或把自评当成官方得分。

---

# 第四部分：Agent 单次任务执行规范

每次给 Agent 一个模块需求文件后，建议强制按以下顺序执行。

## Step 1：理解与只读检查

Agent 先输出：

- 任务目标；
- 本次允许修改的文件范围；
- 预计不修改的关键区域；
- 已发现的相关现状；
- 风险点。

未完成只读检查前，不进行大范围改动。

## Step 2：建立最小修改方案

Agent 应给出：

- 数据流；
- API/Skill 变化；
- 数据库变化（如有）；
- UI 变化；
- 测试变化。

如果能通过扩展现有模块实现，不新建重复体系。

## Step 3：实施

要求：

- 小步修改；
- 每个修改围绕本模块；
- 不夹带无关重构；
- 不顺手升级框架；
- 不修改工作区外文件。

## Step 4：自测

先跑模块级测试，再跑全量回归。

## Step 5：实际链路走查

不能只靠单元测试。

至少按需求文件给出的真实操作步骤走一遍。

## Step 6：最终报告

Agent 最终必须交付：

1. 修改了哪些文件；
2. 每个文件改了什么；
3. 新增/调整了哪些接口或数据结构；
4. 跑了哪些测试及结果；
5. 哪些验收项已通过；
6. 哪些仍未实现；
7. 是否产生迁移/生成文件；
8. 是否存在已知风险。

---

# 第五部分：Agent 禁止事项清单

以下行为默认禁止：

1. 修改工作区外文件。
2. 覆盖原始基线 ZIP。
3. 修改赛题原文或官方附件。
4. 手改 `dist/` 代替正式构建。
5. 为了修一个功能重写整个前端/后端。
6. 未说明就新增大量依赖。
7. 强制把本地项目改成公网 API 依赖。
8. 使用固定数据冒充真实分析结果。
9. 用前端角色变量冒充权限系统。
10. 用 `localStorage` 冒充跨账号持久化。
11. 无依据时伪造课程引用。
12. 将网页当前明细 JSON 作为安全导出的失败兜底。
13. 在日志中写入敏感原文、Token 或 Cookie。
14. 引入真实学校、教师、学生识别信息。
15. 删除数据库或清表来“修复”测试。
16. 删除/跳过核心测试让测试变绿。
17. 把“目标、计划、设计”写成“已完成、已验证”。
18. 把帝王蟹历史验证写成当前实时调用。
19. 未执行测试却报告“测试通过”。
20. 在一个模块任务中夹带与需求无关的大规模重构。

---

# 第六部分：需要人工确认后才能执行的高风险变更

以下情况 Agent 不应自行决定，应在需求文件中明确批准后再做：

- 更换前端技术栈；
- 更换后端运行方式；
- 新增强制公网服务；
- 新增大量第三方依赖；
- 修改 Skill 核心契约；
- 删除或重构现有数据库主表；
- 修改公开部署架构；
- 改变演示账号/身份模型；
- 删除现有功能；
- 大规模改动 `worker/index.js` 与公开版行为；
- 修改所有页面视觉风格；
- 改变已有测试的核心验收标准；
- 引入真实学校数据；
- 引入真实用户认证系统；
- 任何会让“本地无网可演示”失效的变更。

---

# 第七部分：后续需求文件统一模板

确认本总控文件后，每个模块建议按同一模板生成需求文档。

```text
1. 模块背景
2. 当前现状
3. 本次目标
4. 明确不做什么
5. 用户流程
6. 功能需求
7. API / Skill 契约
8. 数据模型 / 持久化
9. 权限与安全
10. UI 状态
11. 异常与降级
12. 允许修改文件范围
13. 禁止修改文件范围
14. 自动化测试
15. 人工验收步骤
16. 完成定义
17. 回归要求
18. Agent 最终报告格式
```

这样每次只把一份需求文件交给 Agent，能明显降低“跨模块乱改”和“做着做着改变项目方向”的风险。

---

# 第八部分：建议确认点

在生成 7 份详细需求文件前，建议项目负责人只确认以下几项：

1. 是否接受 7 个模块的拆分；
2. 是否接受实施顺序 `M2 → M1 → M3 → M4 → M5 → M6 → M7`；
3. M3/M4 的运行时持久化是否继续使用 SQLite；
4. 公开 Worker 版是否本阶段同步补齐业务 API，还是先保证本地 Node 版完整；
5. 是否把本文件直接放到仓库根目录，作为后续 Agent 的长期工作约束；
6. 后续需求文件是否采用“每个模块只允许修改白名单文件”的严格模式。

在上述事项确认前，不建议让 Agent 同时开始多个大模块开发。
