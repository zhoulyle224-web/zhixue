# M3 教学任务与反馈闭环验收报告

日期：2026-09-17  
需求基线：`REQ-M3_教学任务与反馈闭环_20260917.md`  
工作约束：`智学双擎_下一阶段模块拆分与Agent工作约束_20260916.md`  
验收结论：**本地 Node + SQLite 的 M3 核心业务闭环已实现并通过自动化与页面走查；M4 真实会话鉴权、公开静态版写链路和“两个不同浏览器产品”的人工复核不属于本次完成范围。**

## 1. 修改文件

| 文件 | 新增/修改 | 修改原因 | 核心变化 |
|---|---|---|---|
| `db/runtime-schema.sql` | 修改 | 增加 M3 持久化模型 | 新增 plans、versions、assignments、events 四张表及唯一约束/索引 |
| `server/runtime-store.mjs` | 修改 | 让任务服务复用同一 runtime SQLite 连接 | 暴露仅供服务端内部事务使用的连接，不通过 HTTP 暴露 |
| `server/task-service.mjs` | 新增 | 集中实现 M3 业务规则 | 动态草案、学生映射、发布、修订、撤回、完成、反馈汇总、版本历史 |
| `server/local-api.mjs` | 修改 | 暴露 M3 HTTP API | 接入 11 组任务接口并复用现有错误响应、审计和只读基线数据库 |
| `assets/task-client.js` | 新增 | 前端不再用 localStorage 冒充闭环 | 教师和学生 UI 均从任务 API 读取/写入；失败时不假成功 |
| `assets/app.js` | 修改 | 移除旧任务假链路 | 删除本地发布、toggle 完成和固定巩固组过滤；接入任务客户端 |
| `assets/style.css` | 修改 | 支撑任务状态、版本、反馈展示 | 增加运行态横幅、版本列表、完成摘要、反馈输入和离线提示样式 |
| `teacher.html` | 修改 | 落地教师任务闭环 | 动态人数、证据区、截止时间、保存/发布、版本历史、撤回、反馈摘要 |
| `student.html` | 修改 | 落地学生本人任务与历史 | 动态层次、0/1 进度、反馈输入、服务端完成状态、撤回历史 |
| `tests/fixtures/m3/publishable-ai201.csv` | 新增 | 提供可映射的合成验收数据 | 使用 S240101–S240110，预期分层 3/5/2 |
| `tests/m3-task-loop.test.mjs` | 新增 | 覆盖 M3-T01～T28 | 覆盖映射、幂等、版本、反馈、撤回、重启、安全与静态降级 |
| `package.json` | 修改 | 纳入回归门禁 | `npm test` 增加 M3 测试 |

未修改：

- `worker/index.js`
- `db/schema.sql` / `db/schema.ts`
- `data/zhixue_demo.sqlite`
- `assets/demo-data.json`
- Skill 实现和 prompts
- dist/ 等生成物

## 2. 数据库变化

### 2.1 runtime 新增表

1. `runtime_task_plans`
   - 逻辑任务计划；
   - 绑定 `context_key / offering_id / class_id`；
   - 一个 plan 可拥有 v1、v2 等多个版本。
2. `runtime_task_versions`
   - 保存 draft / published / superseded / revoked；
   - 保存 M2 来源证据、分层快照、任务正文、截止时间、内容摘要、发布时间、撤回原因；
   - `(plan_id, version_no)` 唯一；
   - 同一 plan 同时最多一个 published。
3. `runtime_task_assignments`
   - 每个版本、每名学生唯一；
   - 保存稳定 `tier_code`、pending/completed、完成时间和脱敏反馈；
   - 发布和完成均有 clientRequestId 幂等约束。
4. `runtime_task_events`
   - 记录 draft_created、draft_updated、published、superseded、assignment_completed、revoked；
   - metadata 不保存完整学生名单或完整反馈正文。

### 2.2 初始化与迁移

`createRuntimeStore()` 启动时继续执行 `db/runtime-schema.sql`。所有新增语句均使用 `CREATE TABLE/INDEX IF NOT EXISTS`，不会清表或删除历史数据。

### 2.3 事务

- 发布：旧版本 supersede（如有）+ 新版本 published + 10 条 assignments + event，在同一 `BEGIN IMMEDIATE` 事务中提交。
- 撤回：版本 revoked + event，同一事务。
- 学生完成：assignment completed + feedback + event，同一事务。
- 任一步异常均 rollback。

### 2.4 基线数据库

`data/zhixue_demo.sqlite` **未发生变化**。

验收前后 SHA-256：

`BBA0FC13A8332286BE5ACEF3E190FCB3ABF54F92648254C100073E83873922C4`

## 3. M2 对接

- `analysisRunId` 直接使用现有 M2 `POST /api/analyze` 顶层字段及 `data._evidence.analysisRunId`。
- M3 通过 `runtimeStore.getAnalysisRun(analysisRunId)` 读取不可漂移的研判快照。
- 分层读取：
  - `excellent_students → extension`
  - `potential_students → improvement`
  - `struggling_students → consolidation`
- 最薄弱知识点从 `knowledge_analysis` 的 `mastery_rate` 最小项计算。
- 来源证据固化：batchId、文件名、SHA-256、validRows、studentCount、Skill id/version、分析时间。
- 未修改 M2 事实计算；任务完成不会改变 average_score、pass_rate 或知识点掌握度。
- `/api/tasks/feedback-summary` 以“上一轮任务反馈辅助证据”单独返回。

## 4. 实际 API

| 需求接口 | 实现状态 |
|---|---|
| `POST /api/tasks/drafts` | 已实现 |
| `PUT /api/tasks/drafts/:versionId` | 已实现 |
| `POST /api/tasks/drafts/:versionId/publish` | 已实现 |
| `POST /api/tasks/versions/:versionId/revise` | 已实现 |
| `POST /api/tasks/versions/:versionId/revoke` | 已实现 |
| `GET /api/tasks/teacher` | 已实现 |
| `GET /api/tasks/student` | 已实现 |
| `GET /api/tasks/student/history` | 已实现 |
| `POST /api/tasks/assignments/:assignmentId/complete` | 已实现 |
| `GET /api/tasks/versions/:versionId/feedback` | 已实现 |
| `GET /api/tasks/feedback-summary` | 已实现 |

所有接口均返回 `cache-control: no-store`；runtime SQLite 仍不能经静态路径访问。

## 5. 版本语义

- v1：首次基于某个 analysisRun 创建的新 plan + draft。
- 保存草案：只允许 draft；任务内容和 dueAt 持久化。
- 发布：重新读取服务端 draft，重新做学生映射，生成不可变 content digest 与 assignments。
- v2：从 v1 复制内容、截止时间、来源证据和 planId，versionNo 严格 +1。
- v2 发布：若同 plan 有 published，旧版本改为 superseded；旧 assignments 和 completion 仍保留。
- revoke：只把当前 published 改为 revoked，不删除 assignments、完成时间或反馈。
- 撤回后学生 active API 不再返回该版本，history 仍返回。

## 6. 学生映射

实际校验链路：

`analysis identifier → students.student_no → students.class_id → enrollments.offering_id`

发布前要求本次分析样本 100% 映射：

- 学生必须存在；
- 必须属于 teacher context 指定 class；
- 必须处于指定 offering 的修读中 enrollment；
- 不允许顺序匹配、自动创建、静默丢弃或由前端提交学生名单。

M3 fixture 实际结果：

- 分析样本 10；
- 班级学生 30；
- 可映射 10；
- 未映射 0；
- 拓展 3 / 提升 5 / 巩固 2；
- S240101 → consolidation。

## 7. 自动测试

| 命令/门禁 | 结果 |
|---|---|
| 修改前 `npm test` | 通过：48/48；Skill 26/26 |
| 修改后 `npm test` | 通过：56/56；Skill 26/26 |
| `node --check assets/app.js` | 通过 |
| `node --check assets/task-client.js` | 通过 |
| `node --check server/local-api.mjs` | 通过 |
| `node --check server/runtime-store.mjs` | 通过 |
| `node --check server/task-service.mjs` | 通过 |
| 基线数据库哈希门禁 | 通过 |
| runtime DB 静态访问 404 | 通过 |

M3-T01～T28 已纳入 `tests/m3-task-loop.test.mjs`，其中相关连续用例按真实链路合并在同一测试中，覆盖：

- 无研判、context 不一致、分层异常/映射阻断；
- 动态 3/5/2 草案和薄弱知识点；
- publish/complete 幂等；
- 本人任务、错误 offering 不泄露其它任务；
- v1 不可编辑、v2、supersede；
- 完成反馈、PII 脱敏、教师 1/10；
- revoke 后禁止提交、历史与完成反馈保留；
- 服务重启恢复；
- 截止时间、纯文本/XSS 安全；
- 反馈 summary 不修改 M2 分数；
- 前端移除 localStorage 假发布、toggle 完成和固定巩固组过滤。

## 8. 人工页面验收

验收使用临时 `:memory:` runtime DB，未写入开发运行数据库。

| 场景 | 状态 | 证据摘要 |
|---|---|---|
| 生成草案 | 人工通过 | 教师页显示拓展 3、提升 5、巩固 2；薄弱点“智能体与搜索” |
| 保存草案 | 人工通过 | 巩固组改为 A* 练习文本，保存后仍显示编辑内容 |
| 发布 v1 | 人工通过 | 页面显示“已发布 · v1”、10 assignments、内容 hash |
| 学生页面收到 | 人工通过 | student:S240101 显示巩固组 v1 和教师刚编辑文本 |
| 学生完成 | 人工通过 | 页面刷新为 1/1 已完成，显示“已完成并持久化” |
| 教师反馈 | 人工通过 | 1/10、巩固组 1/2、S24***01 和反馈文本 |
| 撤回 | 人工通过 | 教师页显示“已撤回 · v1”，完成摘要仍为 1/10 |
| 学生历史保留 | 人工通过 | active 消失；history 显示 v1 已撤回、已完成、反馈保留 |
| 创建 v2 | 人工通过 | 教师页显示 v2 草案和 v1 已撤回 |
| 发布 v2 | 自动化通过 | v2 published、v1 superseded，学生只看 v2 |
| 服务重启 | 自动化通过 | 同一临时 runtime DB 重启后版本、assignment、feedback 一致 |
| API 故障 | 自动化通过 | 前端无 localStorage 发布/完成后备路径，不显示假成功 |

说明：页面走查使用两个标签页完成教师/学生角色切换；“两个不同浏览器产品”的人工复核未执行。跨会话业务事实不依赖 localStorage，已由 HTTP 集成测试、重启测试和页面 API 数据回流共同验证。

## 9. 完成定义对照

### 自动化已覆盖

D01–D42、D44：均由 API 集成测试、静态前端检查、数据库哈希门禁、语法检查或 M1/M2/Skill 回归覆盖。

重点结果：

- D01–D13：真实 analysisRun → 可编辑草案 → 事务发布 → 10 assignments。
- D14–D22：本人任务、动态 tier、完成持久化、反馈脱敏、幂等。
- D23–D32：不可变版本、v2、supersede、revoke、history、feedback summary。
- D33–D42：不改成绩、不改基线 DB、不暴露 runtime DB、不再以 localStorage 为事实源、回归全部通过。
- D44：页面明确仍是演示 context，没有表述为生产级认证。

### 人工已验证

- 教师生成/编辑/保存/发布；
- 学生获取本人任务并提交完成；
- 教师看到完成摘要与脱敏反馈；
- 撤回后 active 消失且 history 保留；
- 创建 v2 草案。

### 尚未完成

- D43 严格意义上的“两个不同浏览器产品”人工完整链路未执行；当前已完成两个页面标签页走查和跨请求/重启自动化验证。
- M4 尚未实施：当前仍使用 `teacher:<offering>:<class>` / `student:Sxxxxxx` 演示 context，尚未实现服务端真实登录会话和不可伪造角色身份。
- 公开静态版不支持 M3 写链路；静态/API 不可用时只显示不可提交状态，不会使用 localStorage 假闭环。
- Worker 未新增 M3 API。

因此本报告不使用“生产级权限完成”“公开版跨账号同步完成”或“全 LMS 完成”等表述。

## 10. 生成物与运行数据

- 新增 source/fixture/test/report 文件见“修改文件”。
- 未修改 dist/、.next/、.wrangler/ 等生成物。
- 自动化测试使用 `:memory:` 或系统临时目录 runtime DB。
- 页面验收使用 `:memory:` runtime DB，验收服务已关闭。
- 本地审计 JSONL 按现有审计机制追加了本次页面验收事件；未记录完整学生名单、Token、Cookie 或完整反馈正文。

## 11. 已知风险

1. M4 前，context 仍由演示页面提供，服务端只校验业务关系，不能证明浏览器真实拥有该身份。
2. `assets/app.js` 仍保留部分基线历史快照文案（首页/报告展示）；M3 正式 active task 已完全由 runtime API 驱动，但历史演示文案仍应在后续统一标注。
3. 本阶段没有实时推送；教师和学生通过进入页面、刷新按钮或重新载入获取最新状态。
4. 正式公开部署需要后续决定是否为 Worker 实现受控任务 API；当前公开静态站不应宣称支持 M3 写闭环。

## 12. 结论

M3 的本地核心闭环已从“浏览器按钮演示”升级为：

`已确认 M2 研判 → 动态草案 → 教师保存/确认 → 不可变版本 → 学生本人 assignment → 服务端完成/反馈 → 教师汇总 → 撤回留痕 → v2 修订`

核心业务事实均持久化在 runtime SQLite，基线 SQLite 保持只读；页面不再以 localStorage 作为正式任务事实源。
