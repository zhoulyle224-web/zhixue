/** knowledge-base-curator：治理 K0/K1/K2 知识条目并生成可检索索引。 */
(function(global){'use strict';const SKILL_ID='knowledge-base-curator',SKILL_VERSION='knowledge-base-curator@1.0.0';function curate(input={}){const entries=Array.isArray(input.entries)?input.entries:[],seen=new Set(),accepted=[],issues=[];for(const item of entries){const id=String(item.id||item.resource_id||'').trim(),layer=String(item.layer||'').trim();if(!id||!['K0','K1','K2'].includes(layer)||!String(item.title||'').trim()||!String(item.text||'').trim()){issues.push({id:id||'unknown',code:'MISSING_REQUIRED_FIELD'});continue}if(seen.has(id)){issues.push({id,code:'DUPLICATE_ID'});continue}seen.add(id);accepted.push({...item,id,layer,keywords:[...new Set([...(item.tags||[]),...String(item.title).split(/[\s、，/]+/)])].filter(Boolean),indexed_at:new Date().toISOString()})}return{skill_id:SKILL_ID,skill_version:SKILL_VERSION,generated_at:new Date().toISOString(),data_scope:'K0-K2',accepted,issues,ready:accepted.length>0,evidence:{input_count:entries.length,accepted_count:accepted.length}}}global.ZhixueSkillKnowledgeCurator={curate,SKILL_ID,SKILL_VERSION}})(window);
