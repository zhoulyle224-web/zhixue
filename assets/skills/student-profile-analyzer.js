/** student-profile-analyzer：只用授权学习证据形成可解释画像。 */
(function(global){'use strict';const SKILL_ID='student-profile-analyzer',SKILL_VERSION='student-profile-analyzer@1.0.0',num=v=>Number.isFinite(Number(v))?Number(v):null;function analyze(input={}){const points=(input.knowledge_points||[]).map(x=>({...x,value:num(x.value??x.mastery)})).filter(x=>x.value!==null).sort((a,b)=>a.value-b.value),weak=points.slice(0,3),strong=[...points].reverse().slice(0,3),attendance=num(input.attendance_rate),completion=num(input.task_completion_rate),wrong=Number(input.wrong_count||0),evidence=[];if(weak.length)evidence.push({type:'mastery',count:weak.length,items:weak.map(x=>x.name)});if(wrong)evidence.push({type:'wrong_book',count:wrong});if(attendance!==null)evidence.push({type:'attendance',value:attendance});if(completion!==null)evidence.push({type:'tasks',value:completion});let cause='练习不足';if(attendance!==null&&attendance<80)cause='出勤与学习连续性不足';else if(completion!==null&&completion<60)cause='任务执行不足';else if(wrong>5)cause='知识应用不稳定';return{skill_id:SKILL_ID,skill_version:SKILL_VERSION,generated_at:new Date().toISOString(),data_scope:input.scope||'current-student-current-course',profile:{strengths:strong,weaknesses:weak,probable_cause:cause,trend:input.trend||'待积累更多数据',attendance_rate:attendance,task_completion_rate:completion,wrong_count:wrong},evidence,confidence:points.length>=3?'高':'中',limitations:points.length?[]:['缺少知识点掌握记录']}}global.ZhixueSkillStudentProfile={analyze,SKILL_ID,SKILL_VERSION}})(window);
