# 智学双擎 M7 参赛交付验收报告

验收日期：2026-09-18  
验收范围：参赛交付、评分证据、部署材料、干净提交包与最终提交状态。  
边界：本轮未新增业务功能，未修改 M1～M5 业务逻辑，未把未执行的人工步骤标记为通过。

## 1. 最终 M6 证据

- 唯一最终证据 run：`20260917T171802Z_2db5f2b`。
- 产品自动化：175/175 通过，fail=0，skip=0，todo=0。
- Skill 校验：26/26 通过。
- Acceptance Matrix：pass=169，fail=0，not_run=0，blocked=8。
- baseline SQLite SHA-256：`bba0fc13a8332286be5acef3e190fcb3abf54f92648254c100073e83873922c4`，保持不变。
- completion checker：`NOT_COMPLETE`。8 个 blocked 项为 MANUAL-M1-01、MANUAL-M1-02、MANUAL-M2-02、MANUAL-M3-01、MANUAL-M3-02、MANUAL-M4-01、MANUAL-M4-02、MANUAL-M5-01。

## 2. 交付文档更新

| 原问题 | 新状态 | 证据 |
| --- | --- | --- |
| README、部署、测试、演示口径分散 | ready | `README.md`、`docs/参赛交付总览.md`、`docs/部署手册.md`、`docs/测试与验收摘要.md` |
| 评分点缺少统一映射 | ready | `docs/评分证据矩阵.md` |
| 平台、导出能力表述可能超出证据 | ready | `docs/能力边界声明.md` |
| 最终提交检查缺少可执行清单 | ready | `docs/最终验收与提交检查单.md` |
| 视频成片缺失 | not_ready | 仅有 `docs/演示视频脚本.md` |
| PPT 成品缺失 | not_ready | 仅有 `docs/PPT大纲.md` |

## 3. 学校信息与敏感信息扫描

- 提交检查会扫描普通文本、文件名及 baseline SQLite 文本列；本次 SQLite 扫描覆盖 129 个文本列。
- 已知禁止组织名称与 URL 命中均为 0；`data/README.md` 已中性化。
- runtime DB、WAL/SHM、Session、audit、日志、真实 `.env` 与 secret pattern 命中均为 0。
- 外部 URL 会输出人工复核列表；依赖锁文件中的 registry、赞助链接与测试占位域名不等同于学校信息。
- PPT、视频二进制尚不存在，因此其画面与媒体元数据人工扫描状态均为 not_ready，不宣称已完成。

## 4. 六项评分证据矩阵

| 评分项 | 状态 | 证据入口 |
| --- | --- | --- |
| 场景适配性 | ready | 教师研判与学生学习闭环；M6 final run；`docs/评分证据矩阵.md` |
| 功能实用性 | ready | M1～M5 自动化、页面可见流程与 clean smoke |
| 操作便捷性 | ready | README、一键启动脚本、教师/学生演示路径 |
| 代码规范性 | ready | 175/175、静态检查、Skill 26/26、manifest |
| 异常处理能力 | ready | M4/M5 权限、CSRF、降级与越权回归 |
| 文档完整性 | blocked | 代码与 Markdown 材料齐全，但最终 PPT、视频及 8 项人工证据未完成 |

本表只描述证据状态，不推算或声称官方得分。

## 5. 部署复核

- Node 本地部署：ready；已从干净 staging 启动并通过 health 与核心业务 smoke。
- Windows 一键启动：ready；`start-local.ps1` 已在 8091 端口实际启动、通过 health、停止并清理临时 runtime。
- Linux：not_ready；已有命令与文档，当前 Windows 验收环境未实际复现。
- Docker：not_ready；已有配置与文档，当前验收未实际构建运行。

## 6. 干净 staging 冒烟

`release/智学双擎_参赛提交版/` 已通过：

- 服务启动与 `/api/health`；
- teacher / student 登录；
- M1 QA；
- M2 validate → confirm → analyze；
- M3 draft → save → publish → student active task；
- M5 受控导出；
- `/data/*.sqlite` 静态访问返回 404；
- 冒烟过程中生成的临时 runtime 已清理。

## 7. 视频状态

状态：not_ready。演示视频脚本、镜头顺序、口径与证据引用已准备；最终视频文件尚未录制和复核。

## 8. PPT 状态

状态：not_ready。PPT 内容大纲、评分映射和演示数据口径已准备；最终 `.pptx` 尚未制作和复核。

## 9. 最终提交包

- staging：`release/智学双擎_参赛提交版/`。
- ZIP：`release/智学双擎_参赛提交版.zip`。
- manifest：`release/release-manifest.json` 及提交包内同名 manifest。
- ZIP SHA-256：记录于 ZIP 同级 `release/SHA256SUMS`，避免在包内嵌入自身哈希。
- ZIP 将从 staging 生成，并执行独立解压后的启动与核心 smoke；结果记录在 `release/submission-check-final.json`。

## 10. 剩余 blocked / not_ready

- blocked：M6 的 8 项真实人工验收仍未完成。
- not_ready：最终 PPT 二进制。
- not_ready：最终演示视频。
- not_ready：Linux 与 Docker 尚未在对应环境实际复现；不影响 Windows/Node 已验证事实，但不能写成已验证。

## 11. 最终结论

自动化、Skill、干净提交包、文档和评分证据已形成；由于 8 项 M6 人工验收仍 blocked，且最终 PPT 与视频二进制缺失，当前不满足 M7 完成定义。

**M7 not ready for submission**
