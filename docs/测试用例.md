# 测试用例与验收入口

## 唯一事实来源

M6 final run：`20260917T171802Z_2db5f2b`。

| 指标 | 结果 |
|---|---|
| Node 产品测试 | 175/175，fail 0，skip 0，todo 0 |
| Skill | 26/26 |
| M6 E2E | 10/10 |
| Acceptance Matrix | pass 169 / fail 0 / blocked 8 |
| baseline | 前后 SHA-256 一致 |
| completion checker | NOT_COMPLETE |

## 命令

```bash
npm test
npm run test:static
npm run test:acceptance
node scripts/verify-skills.cjs
node scripts/prepare-submission.mjs
node scripts/check-submission.mjs --target release/智学双擎_参赛提交版
```

## 模块覆盖

| 模块 | 主要自动化 |
|---|---|
| M1 | 课程隔离、引用、待办、回复、PII、幂等、重启 |
| M2 | CSV/JSON 质检、confirm、analysis、错误定位、恢复 |
| M2.5 | analysis immutable contract、latest 与 source evidence |
| M3 | 草案、发布、完成、反馈、撤回、v2、终态状态机 |
| M4 | Session、CSRF、角色与对象授权、重启恢复 |
| M5 | POST export、白名单、脱敏、水印、strict audit、格式 |
| M6 | 10 条跨模块 E2E、静态安全、baseline hash |
| M7 | staging、敏感信息扫描、manifest、clean smoke、ZIP/hash |

## 必须保留的人工 blocked

- MANUAL-M1-01、MANUAL-M1-02
- MANUAL-M2-02
- MANUAL-M3-01、MANUAL-M3-02
- MANUAL-M4-01、MANUAL-M4-02
- MANUAL-M5-01

这些项目不能由 API 或自动化测试“代替人工改绿”。具体步骤见 final evidence 的 `manual/checklist.md`。

## Clean staging 冒烟

M7 在临时 runtime SQLite 上真实启动 staging server 并检查：

1. `/api/health`；
2. teacher/student 登录；
3. M1 问答；
4. M2 validate→confirm→analyze；
5. M3 draft→save→publish→student fetch；
6. M5 正式 export；
7. `/data/zhixue_demo.sqlite` 为 404。

冒烟只证明 clean package 可启动，不替代完整 M6 或人工验收。
