# M1 学生答疑与课程证据链验收报告

日期：2026-09-17  
依据：`REQ-M1_学生答疑与课程证据链_20260917.md`、项目工作约束  
结论：**代码实现、自动化门禁及双标签独立会话页面联动通过；严格的双浏览器/Profile 与页面级服务重启签收未完成，因此不宣称 M1 全部完成。**

## 1. 修改文件

| 文件 | 变更 | 原因与核心变化 |
|---|---|---|
| `db/runtime-schema.sql`、`server/runtime-store.mjs` | 修改 | 在 M2 同一运行时 SQLite 中新增 `runtime_qa_records`、唯一约束、学生/教师查询索引及问答/回复操作；未写基线业务 DB。 |
| `server/course-knowledge.mjs` | 新增 | 按服务端课程代码加载知识包；校验 schema、资源课程归属与 `learning_resources` 关联；无效单资源隔离、manifest 整体错误阻断。 |
| `server/qa-service.mjs` | 新增 | 从只读 DB 推导 student→enrollment→offering→course；装配当前课程 KB、二次验证引用、持久化问答/待办、按上下文查询和教师回复。 |
| `server/local-api.mjs` | 修改 | 增加 M1 五个接口；空问题、长度、注入和 PII 门禁；为故障注入测试支持 runtime store 注入。 |
| `assets/skills/course-ai-tutor.js` | 修改 | 保留主输入输出契约；去掉隐式固定 KB；答案、方法、追问仅来自本次注入资料；返回结构化 evidence 与稳定版本。 |
| `assets/knowledge/manifest.json`、`assets/knowledge/CS201/*.json`、`assets/knowledge/AI201/*.json` | 新增 | 两门课程共六份明确标注的合成演示资料，含稳定资源/片段 ID 与可定位章节。 |
| `assets/qa-client.js` | 新增 | 学生动态资料、快捷问题、正式问答/历史；教师服务端收件箱和回复；切课请求令牌与发送中按钮门禁；静态版失败时不伪称同步。 |
| `assets/app.js`、`student.html`、`teacher.html`、`assets/style.css` | 修改 | 连接 M1 客户端；移除答疑区固定 12 份资料/模型评估内容；不再把本地 `state.questions` 当正式教师待办或正式回复。 |
| `tests/m1-course-qa.test.mjs`、`tests/local-api.test.mjs`、`package.json` | 新增/修改 | 增加 M1-T01～T20 与知识包/页面回归；旧安全测试改为正式课程绑定请求。 |
| `scripts/verify-skills.cjs` | 修改 | 按需求 §161 改为显式注入测试 KB，保留原 26 项契约检查；未跳过测试。 |

未改动 Worker、M2 分析逻辑、其它 Skill、公开快照、基线业务 SQLite；未新增生产依赖。

## 2. 知识包

| 课程 | 代码 | 有效资源数 | 资源 ID | 版本 | 标识 |
|---|---|---:|---|---|---|
| 数据结构 | CS201 | 3 | `CS201-linear-list-v1`、`CS201-stack-queue-v1`、`CS201-tree-v1` | manifest `2026.09`；资源 `demo-2026.09` | `synthetic: true`，合成演示课程资料 |
| 人工智能导论 | AI201 | 3 | `AI201-agent-search-v1`、`AI201-knowledge-representation-v1`、`AI201-ml-concepts-v1` | 同上 | 同上 |

每份资源的 `dbResourceId` 均经只读基线 DB 校验；正文由项目内 JSON 提供，基线 `learning_resources` 只承担身份与课程归属证据，不宣称其包含讲义全文。其它未配置资料的课程返回资源数 0，提问转教师，不借用两门已配置课程。

## 3. API 实际状态

| 接口 | 状态 | 关键约束 |
|---|---|---|
| `POST /api/qa` | 已实现/自动测试通过 | `studentContext + offeringId` 校验、当前课程 KB、结构化引用、持久化、幂等；待办写失败返回 `QA_HANDOFF_FAILED`。 |
| `GET /api/qa/resources` | 已实现/自动测试通过 | 按选修课程返回实际有效资源数；无包返回空列表。 |
| `GET /api/qa/history` | 已实现/自动测试通过 | 当前学生/教学班限定；默认 20、最大 50。 |
| `GET /api/qa/teacher-inbox` | 已实现/自动测试通过 | `teacher:<offering>:<class>` 隔离；默认 50、最大 200；学生编号脱敏。 |
| `POST /api/qa/:id/reply` | 已实现/自动测试通过 | context 与问题一致、人工回复非空且不超过 2000 字、PII 清理后持久化。 |

M1 的 context 校验是**演示业务数据隔离**，不是生产级身份认证。

## 4. runtime DB 与基线完整性

- 新表：`runtime_qa_records`。保存脱敏问题、course/offering/class、Skill 答案与 evidence、待办与教师回复、版本、时间戳；不保存原始手机号/邮箱。
- 约束：`UNIQUE(student_context, client_request_id)`；索引：`idx_runtime_qa_student`、`idx_runtime_qa_inbox`。
- 初始化：沿用 `createRuntimeStore()` 的幂等 SQL 初始化，复用 `data/runtime/zhixue_runtime.sqlite`；测试使用 `:memory:` 或 `os.tmpdir()` 临时 runtime DB，不触碰用户运行历史。
- 基线 `data/zhixue_demo.sqlite`：**未修改**。操作前后 SHA-256 均为 `BBA0FC13A8332286BE5ACEF3E190FCB3ABF54F92648254C100073E83873922C4`。
- `/data/runtime/zhixue_runtime.sqlite` 静态访问测试返回 404；运行时问答未写入 `assets/demo-data.json` 或其它公开快照。`.gitignore` 原有 runtime SQLite 忽略规则保持有效。

## 5. Skill 契约与证据

输入继续支持 `student_question`、`course_name`、`knowledge_base`、`question_context`、`student_level`；输出保留 `answer_status`、`answer_content`、`related_knowledge`、`guide_questions`、`_refs`。空 KB 必定待人工；追问取自命中片段的 `guideQuestions`，回答只组织片段的 `text/method/summary`。Skill 输出 `_evidence` 后，服务端再用**本次注入的当前课程 KB**核对 resourceId、chunkId、locator、version、dbResourceId 与引用顺序。稳定版本为 `course-ai-tutor@local-1.1`。

## 6. 自动测试与语法门禁

| 检查 | 实际结果 |
|---|---|
| `npm test` | **通过：48/48 Node 测试；26/26 Skill 契约测试** |
| `node scripts/verify-skills.cjs` | **通过：26/26**（亦由 `npm test` 执行） |
| `node --check` | **通过**：`assets/app.js`、`assets/qa-client.js`、`assets/skills/course-ai-tutor.js`、`server/local-api.mjs`、`server/runtime-store.mjs`、`server/course-knowledge.mjs`、`server/qa-service.mjs` |
| M1-T01～T04 | **通过**：CS/AI 有据回答、跨课不借资料、无包不回退 |
| M1-T05～T08 | **通过**：正确/错误教师 context、人工回复入历史、重启恢复 |
| M1-T09～T12 | **通过**：PII 先脱敏、注入无记录、引用定位、幂等 |
| M1-T13～T16 | **通过**：基线哈希、runtime 不静态暴露、追问课程化、空 KB pending |
| M1-T17～T20 | **通过**：未选修禁止、错配资源排除、写待办故障 503、安全回归 |
| 额外知识包/页面测试 | **通过**：JSON 字段与唯一性、学生答疑区固定资料文案移除 |

测试中的“跳过错配资源”和“QA 持久化失败”日志来自**故意构造的故障用例**，对应测试均通过。

## 7. 人工验收状态

| 场景 | 状态 | 说明 |
|---|---|---|
| CS201 页面切换、资料/快捷问题、正常有据回答 | **通过（浏览器页面）** | 观察到 3 份数据结构资料、栈/队列/二叉树快捷问题；回答引用《栈与队列》§2.1，展开显示合成演示标识及版本。 |
| AI201 页面切换、A* 正常有据回答 | **通过（浏览器页面）** | 观察到 3 份 AI201 资料及 A* 等快捷问题；A* 回答引用《智能体与搜索》§1.3。 |
| CS201→AI 问题、AI201→CS 问题跨课阻断 | **部分通过** | API 自动测试覆盖双向；页面手工验证了 AI201 问栈/队列转教师，CS201 问 A* 的页面操作未单独执行。 |
| 无资料课程页面与转教师 | **通过（浏览器页面）** | 计算机组成原理显示 0 份资料，提问后显示资料不足与真实问题编号。 |
| 第二个会话的教师待办与错误班级隔离 | **通过（双标签独立 SessionStorage）** | 学生、教师分别使用不同标签且不勾选设备保持登录；教师默认错误班级为空，切换至 AI201/计科1班后看到学生待办。**不是两个独立浏览器/Profile。** |
| 教师回复后刷新、学生刷新看到回复 | **通过（浏览器页面）** | 教师人工回复并刷新，仍为“教师已回复”；学生刷新后在原问题下看到单独标注的“教师回复”。 |
| 服务重启页面恢复 | 未运行 | 同一临时 runtime DB 的 API 重启测试已通过；页面级服务重启未执行。 |
| API 不可用时前端降级 | **通过（浏览器页面）** | 停止本地 Node 后再次发送，页面显示“本地答疑服务不可用……教师待办尚未同步”，按钮恢复，不新增虚假成功记录。 |

因此需求 D35 的**严格双浏览器/Profile**与页面级重启项尚未满足。建议在本地 Node 运行 `npm run dev` 后，用普通窗口登录学生演示账号、独立隐身窗口登录教师演示账号，再按需求 §115–123 完成最终签收。上述双标签测试使用临时内存 runtime DB，不污染用户正式答疑历史；浏览器模拟登录仅是演示流程，不能当作真实身份验证。

## 8. 未完成项与边界

1. **M1 严格双浏览器/Profile 人工签收与页面级服务重启未完成**；双标签独立会话已验证主交互，但当前报告仍不标记“全部完成”。
2. M4 服务端会话、角色与授课关系的真实认证授权仍未实现；不能将 M1 context 校验视为生产安全边界。
3. 公开 Worker 仍未实现写入式问答/跨账号同步；静态版只做诚实错误提示。此次未发布 Git 或公开网站（本次请求未要求）。
4. 除 CS201、AI201 外尚未配置课程知识包，这些课程转教师处理。
5. M3 的教学任务发布与完成回流是另一模块；本次仅打通“提问→教师回复”的答疑反馈。
6. 学生首页的旧“模型评估”欢迎卡、学习路径及任务文案仍是既有演示快照；本次仅将 **M1 答疑区**按课程动态化。后续应在对应模块统一清理这些跨课程不一致文案。学生端现有学习记录导出也仍属于旧快照口径，未擅自扩为运行时 QA 正式导出（M5 范围）。

**最终判定：M1 的本地服务端课程证据链、自动化测试与前端接线已交付；必须完成双浏览器页面签收后，才能将整个 M1 标记为验收完成。**
