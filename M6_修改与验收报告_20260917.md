# M6 自动化测试与验收证据：修改与验收报告

日期：2026-09-17

需求基线：`REQ-M6_自动化测试与验收证据_20260917.md`

被测代码提交：`ccb5555`

最终 evidence run：`20260917T153031Z_ccb5555`

## 1. 修改文件

| 文件 | 新增/修改 | 用途 |
|---|---|---|
| `package.json` | 修改 | `npm test` 改为跨平台自动发现；增加 `test:static`、`test:acceptance`。 |
| `scripts/run-tests.mjs` | 新增 | 递归发现产品 `*.test.mjs`；明确排除与智学双擎正式产品无关的 Vinext starter 模板测试。 |
| `scripts/run-static-checks.mjs` | 新增 | 对产品 JavaScript 执行语法门禁。 |
| `scripts/check-public-safety.mjs` | 新增 | 扫描公开文件中的敏感内容，并验证 Worker 受保护接口 fail closed。 |
| `scripts/run-acceptance.mjs` | 新增 | 运行测试、Skill、静态安全检查，校验 baseline hash，生成结构化 evidence。 |
| `scripts/check-m6-completion.mjs` | 新增 | 根据最终 evidence 判断 M6 是否达到完整 DoD。 |
| `scripts/start-m6-manual.mjs` | 新增 | 使用独立临时 runtime SQLite 启动人工验收服务；可通过进程参数注入严格审计失败，不暴露测试 HTTP 后门。 |
| `tests/acceptance-matrix.json` | 新增 | 收录 M1～M5 原需求项、M6-E2E-01～10 与 12 项人工验收。 |
| `tests/m6-cross-module.test.mjs` | 新增 | 跨模块 E2E-01～10。 |
| `tests/m6-acceptance-infrastructure.test.mjs` | 新增 | 验证矩阵完整性、状态枚举、测试追踪与人工项不自动改绿。 |
| `tests/manual-acceptance-results.json` | 新增 | 记录本次真实页面人工走查的操作、预期、实际与结果。 |
| `tests/m2-import-analysis.test.mjs` | 修改 | 为既有 M2 测试补齐 M2-T01～T12 追踪 ID；未降低断言。 |
| `tests/m5-export-security.test.mjs` | 修改 | M5-T40 从硬编码 package 测试名改为验证自动发现 runner 与模块测试文件存在。 |
| `M6_修改与验收报告_20260917.md` | 新增 | 本报告。 |

未修改 M1～M5 业务服务、数据库 schema、Skill、Worker 业务逻辑、baseline SQLite 或生成物。

## 2. 修改前基线

- 原始 20260914 历史基线：Node/API/页面基础测试 9/9，Skill 26/26（需求文件记录，仅作历史参考）。
- M6 开工时最新工作区实际结果：Node 产品测试 155/155，Skill 26/26。
- M6 开工前 baseline SQLite SHA-256：`bba0fc13a8332286be5acef3e190fcb3abf54f92648254c100073e83873922c4`。
- M6 开工前先上传当前版本：提交 `24d63e9`。

## 3. 测试架构

- G0：环境、Git 提交与 baseline SQLite 前后哈希。
- G1：本地 API、页面和原基础回归。
- G2：M1 课程证据链、待办、回复、脱敏、幂等与持久化。
- G3：M2/M2.5 真实导入、逐行质检、固定研判、历史与重启。
- G4：M3 草案、发布、assignment、completion、feedback、撤回与版本状态机。
- G5：M4 Session、Cookie、CSRF、RBAC/ABAC、IDOR、重启与静态暴露。
- G6：M5 受控导出、严格审计、脱敏、水印、格式安全；M6 跨模块 E2E 与公开安全检查。
- G7：真实页面人工验收，结果与自动化分开记录。

测试服务均使用内存或临时 runtime SQLite。M6 E2E 使用会话 Cookie/CSRF 客户端；审计失败通过内部 dependency injection 触发。未新增测试框架或第三方依赖。

## 4. Acceptance Matrix

- requirement items：177
- 自动化已覆盖：165
- 人工已验证：3
- 尚未实现：9
- automated items：165
- manual items：12
- blocking items：112
- 本次动态结果：pass 168、fail 2、blocked 7、not_run 0

`coverageStatus` 与本次动态运行结果分离；只有真实人工通过的 MANUAL-M2-01、MANUAL-M5-02、MANUAL-M5-03 标为“人工已验证”。

## 5. 自动化结果

- `npm test`：169/169 pass，fail 0，skip 0，todo 0。
- Skill contract：26/26 pass。
- `npm run test:static`：33 个 JavaScript 文件语法通过；29 个公开文件安全检查通过；Worker 正式导出 fail closed。
- `npm run test:acceptance`：自动化门禁 PASS。
- baseline SQLite：前后哈希一致。
- acceptance runner：exit 0（自动化与 hash 门禁通过）。
- completion checker：`NOT_COMPLETE`（2 项人工 fail、7 项人工 blocked）。

## 6. M6-E2E

| ID | 结果 | 验证内容 |
|---|---|---|
| M6-E2E-01 | pass | 两批真实导入产生不同固定研判。 |
| M6-E2E-02 | pass | 无依据问题进入对应教师待办。 |
| M6-E2E-03 | pass | 教师回复回到同一学生历史。 |
| M6-E2E-04 | pass | 研判发布、学生完成、教师反馈闭环。 |
| M6-E2E-05 | pass | 撤回仅移出 active，历史与完成信息保留。 |
| M6-E2E-06 | pass | 学生伪造教师请求在 M1/M2/M3/M5 均被拒绝。 |
| M6-E2E-07 | pass | 教师跨班 context 在 M1/M2/M3/M5 均被拒绝。 |
| M6-E2E-08 | pass | strict audit 故障无文件且无静态回退。 |
| M6-E2E-09 | pass | runtime/baseline/audit 不公开，静态页无 Session。 |
| M6-E2E-10 | pass | 同一 runtime DB 重启后 QA、研判、任务完成可读。 |

## 7. 人工验收

| ID | 结果 | 实际结论 |
|---|---|---|
| MANUAL-M1-01 | blocked | 资料不足转人工可见，但未完成课程切换后的有据回答。 |
| MANUAL-M1-02 | blocked | pending→教师回复→学生刷新成功；仅为同一 IAB 的两个 Origin，不符合独立浏览器/Profile。 |
| MANUAL-M2-01 | pass | A 为 68/70%/2-5-3，B 为 92.5/100%/10-0-0，analysis 标识变化。 |
| MANUAL-M2-02 | blocked | 异常逐行定位和刷新保持已验证，未在导入页完成离线子场景。 |
| MANUAL-M3-01 | blocked | 发布→完成→反馈实际成功；严格双浏览器条件未满足。 |
| MANUAL-M3-02 | blocked | IAB 未暴露撤回 prompt/confirm，未继续伪造撤回/v2。 |
| MANUAL-M4-01 | blocked | 学生访问教师页被重定向；未完成独立浏览器与 DevTools 两条 403 请求。 |
| MANUAL-M4-02 | blocked | 教师/学生 logout 后受保护页不可继续；未执行 localStorage 篡改。 |
| MANUAL-M5-01 | fail | 教师 JSON/CSV/Excel 成功；打印页被弹窗策略阻止；学生 learning-record 两种范围均 403。 |
| MANUAL-M5-02 | pass | Node 停止与严格审计失败均明确不下载、不回退。 |
| MANUAL-M5-03 | pass | 公开合成样例显式下载入口与正式导出分离。 |
| MANUAL-M6-01 | fail | 重启后 Session、QA、任务、completion 保持；研判页未恢复当前 analysis 展示。 |

详细操作、预期和实际见 `evidence/m6/20260917T153031Z_ccb5555/manual/checklist.md`。

## 8. 发现并修复的问题

### 8.1 M5-T40 与自动发现 runner 的合法契约调整

- 发现：`npm test` 改为 `scripts/run-tests.mjs` 后，旧 M5-T40 仍只匹配 package.json 中硬编码的 M1～M4 文件名。
- 失败证据：首次全量 M6 回归仅 M5-T40 失败。
- 修复文件：`tests/m5-export-security.test.mjs`。
- 新契约：确认 package script 调用自动发现 runner、runner 发现 `*.test.mjs`、M1～M4 产品测试文件实际存在。
- 合法原因：REQ-M6 要求跨平台自动发现，测试覆盖范围未降低。
- 回归结果：169/169 pass。

### 8.2 M2 测试追踪 ID

- 发现：已有 M2 自动化断言充分，但名称不能直接追踪 M2-T01～T12。
- 修复文件：`tests/m2-import-analysis.test.mjs`。
- 变化：仅补测试 ID，不更改断言与业务契约。

本轮未修改业务代码。人工走查发现的学生 learning-record 403 和重启后研判页展示问题保留为失败证据，没有为了 M6 变绿扩大 M5/M2 业务修改。

## 9. 未完成项

- blocked：MANUAL-M1-01、M1-02、M2-02、M3-01、M3-02、M4-01、M4-02。
- fail：MANUAL-M5-01、MANUAL-M6-01。
- flaky：0。
- 严格双浏览器人工验收尚未完成：当前环境只提供一个 Codex IAB 后端，使用 127.0.0.1/localhost 隔离 Cookie 不能替代不同浏览器/Profile。
- 学生 learning-record 页面正式导出返回 403；打印页被 IAB 弹窗策略阻止。
- 服务重启后任务页保留 analysis 来源，但学情研判页没有恢复当前分析展示。

因此不得写“M6 已全部完成”。

## 10. Evidence

最终证据目录：`evidence/m6/20260917T153031Z_ccb5555/`

- `environment.json`
- `acceptance-results.json`
- `summary.md`
- `commands/tests.txt`
- `commands/skills.txt`
- `commands/syntax.txt`
- `commands/public-safety.txt`
- `manual/checklist.md`

证据不包含 Session Cookie、CSRF、密码或正式导出文件。

## 11. Baseline DB

`data/zhixue_demo.sqlite` 未变化。

- before：`bba0fc13a8332286be5acef3e190fcb3abf54f92648254c100073e83873922c4`
- after：`bba0fc13a8332286be5acef3e190fcb3abf54f92648254c100073e83873922c4`
- unchanged：true

## 12. Runtime 数据

- 自动测试没有写入真实开发 runtime DB。
- 自动化使用内存或操作系统临时目录。
- 人工验收使用独立 `tests/.tmp/m6-manual.sqlite`。
- 未删除历史业务数据、assignment、completion、feedback、event 或审计记录。
- 无 schema 变更，无迁移文件。

## 13. 网络

- 自动测试与 acceptance runner 不需要外网，network policy 为 `offline-only`。
- fixtures 全部为本地合成数据。
- Git 推送是交付动作，不属于测试依赖。

## 14. Test skips

- blocking skip：0
- todo：0
- cancelled：0
- 未通过项均保留为 fail/blocked，没有删除、skip 或自动重试到绿。

## 15. 最终结论

**M6 未完成。**

自动化部分已完成：169/169 Node 产品测试、26/26 Skill、10/10 M6 E2E、静态安全门禁和 baseline hash 全部通过。

完整 DoD 仍被 9 项必需人工验收阻塞，其中 2 项为真实页面失败、7 项为步骤或独立浏览器环境未满足。后续 M7 不得把这些项引用为“已通过”。

---

## 16. P0 缺陷修复复验补充（2026-09-18）

本节记录 commit `943a95e` 对总体验收中两个真实页面缺陷的最小修复；前述 `20260917T153031Z_ccb5555` 证据保持不变，用于保留修复前证据链。

### 16.1 学生 learning-record 403

- 根因：同一 Session 的其他标签页调用 `/api/auth/me` 后会安全轮换 CSRF，旧标签页仍持有失效 token；导出客户端把 `AUTH_CSRF_INVALID` 统一显示成范围无权，造成当前课程和全部课程看似都被权限拒绝。
- 修复：`assets/api-client.js` 仅在受保护写请求收到 `AUTH_CSRF_INVALID` 时调用一次 `/api/auth/me`，取得新 CSRF 后重试一次；不关闭 CSRF、不持久化 token、不放宽权限。
- 授权：`server/export-service.mjs` 继续只从 Session actor 获取学生身份；`offeringId` 仅作范围且必须通过 enrollment 校验；空 scope 只汇总当前学生有效 enrollments；注入 studentId/studentContext/context 等身份字段明确返回 403。
- 人工复验：当前课程与全部授权课程 JSON 均显示受控文件已生成，并分别产生 exportId/watermark。Print 未修改，仍留待正常 Chrome/Edge 复核，因此 `MANUAL-M5-01=blocked`。

### 16.2 重启后研判页面恢复

- 根因：`/api/import/latest` 只返回最新上传批次；较新的未确认批次会遮挡此前已确认批次的 completed analysis，前端因内存丢失而显示未研判。
- 修复：runtime store 增加按 context 读取最近 confirmed batch 的 completed analysis；latest 合同保留当前最新批次，同时返回 `latestCompleted` 正式快照。页面优先使用当前批次 analysis，否则恢复该正式快照，并分别保存 `analysisBatch` 与当前导入批次。
- scope：切换课程/班级先清空旧 analysis，再按新 context 读取；无历史时显示真实空状态；不调用 `/api/analyze`、不使用 localStorage 或 demo-data 恢复。
- 人工复验：服务重启前后均为 `analysis_d7c8f0cd-de06-494c-9733-8635b42e768e`，平均分 68、及格率 70%、2/5/3 分层、知识点、建议和 evidence 一致；切换无历史班级为空，切回恢复同一 ID。`MANUAL-M6-01=pass`。

### 16.3 测试与新证据

- 新增 `tests/m6-p0-fixes.test.mjs`：M6-FIX-01～05 全部通过。
- `npm test`：175/175 pass，fail 0，skip 0，todo 0。
- `npm run test:static`：34 个 JavaScript 文件语法通过；29 个公开文件安全检查通过。
- `npm run test:acceptance`：自动门禁 PASS，Matrix pass 169 / fail 0 / blocked 8。
- `node scripts/verify-skills.cjs`：26/26 pass。
- baseline SHA-256 前后均为 `bba0fc13a8332286be5acef3e190fcb3abf54f92648254c100073e83873922c4`。
- 新 evidence run：`evidence/m6/20260917T161508Z_943a95e/`。
- completion checker：`NOT_COMPLETE`；仍有 8 项人工 blocked，未宣称 M6 全部完成。

### 16.4 当前剩余 blocked

`MANUAL-M1-01`、`MANUAL-M1-02`、`MANUAL-M2-02`、`MANUAL-M3-01`、`MANUAL-M3-02`、`MANUAL-M4-01`、`MANUAL-M4-02`、`MANUAL-M5-01`。

其中 `MANUAL-M5-01` 仅剩正常 Chrome/Edge 的 Print HTML 人工复核；本轮没有为 IAB popup 策略修改正式打印逻辑。无 fail、无 flaky、无 schema 变更、无已有数据迁移，自动化与页面复验均未写真实开发 runtime DB。
